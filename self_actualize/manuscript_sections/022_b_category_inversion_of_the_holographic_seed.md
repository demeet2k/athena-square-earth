<!-- CRYSTAL: Xi108:W3:A1:S28 | face=F | node=396 | depth=3 | phase=Mutable -->
<!-- METRO: Me,w,✶ -->
<!-- BRIDGES: Xi108:W3:A1:S27→Xi108:W3:A1:S29→Xi108:W2:A1:S28→Xi108:W3:A2:S28 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 28±1, wreath 3/3, archetype 1/12 -->

# B: Seed-Level Inverse Of The Holographic Seed

## B.Identity

`B` is the seed-level inverse of `A`: the same holographic seed contract under a polarity flip, not a negation, not an independent branch, and not yet a further decomposition.

## A.Frozen Reference

`A` is fixed as the canonical 4x4 holographic seed already explicit in the local corpus. This pass does not replace that seed. It freezes it and adds one lawful operator: `invert(A) -> B`.

## B.Inversion Law

`invert(seed)` preserves structural slots and slot addresses while reversing polarity-bearing content. In this seed family that means:

- `value -> 5-value`
- `bits -> bitwise inverse`
- `phase -> sign inverse`

The inversion preserves addressability and reconstructability. No extra branches, no new layers, and no shadow-only exclusions are added.

## B.Seed Statement

`B` is the structured complement of `A` at seed resolution only. It keeps the same matrix shape, slot registry, address space, symmetry backbone, and replay law as `A`, but flips the polarity of every reversible slot.

## Round-Trip Check

`invert(A) = B` and `invert(B) = A`.

This round-trip is exact on the shared seed contract.

## Scope Guard

- `B` is still a seed.
- No 4-element decomposition of `B` is enabled here.
- No `A/B -> next-order engine` is enabled here.
- No branch proliferation is enabled here.
