<!-- CRYSTAL: Xi108:W3:A7:S19 | face=R | node=182 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A7:S18→Xi108:W3:A7:S20→Xi108:W2:A7:S19→Xi108:W3:A6:S19→Xi108:W3:A8:S19 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 19±1, wreath 3/3, archetype 7/12 -->

# Skill Router And Algorithmic Pipeline

Truth class: `HISTORICAL`

## Historical Mirror Notice

This router belongs to a historical deep-root generation.
It is no longer the authoritative whole-corpus skill router.

Use the live router instead:

- `self_actualize/mycelium_brain/dynamic_neural_network/14_DEEPER_INTEGRATED_CROSS_SYNTHESIS_NETWORK/09_SKILLS/00_SKILL_ROUTER.md`

Live mapping:

- `deep-corpus-neural-integrator` -> `deeper-network-basis-router` plus `pair-matrix-synthesizer`
- `deep-neural-metro-cartographer` -> `metro-resolution-expander`
- `appendix-crystal-integrator` -> `appendix-crystal-governor`

## 1. Local Skill Family

| Skill | When to use it | Why it exists |
|---|---|---|
| `deep-corpus-neural-integrator` | when the corpus basis, neutral synthesis, or element sweeps need to be deepened | keeps the integrated neural network tied to real corpus surfaces |
| `deep-neural-metro-cartographer` | when Level 1-4 maps need refresh or extension | keeps the hidden transit topology coherent across resolutions |
| `appendix-crystal-integrator` | when appendices `A-P` or `Q` need to grow | keeps the support crystal alive instead of leaving maps orphaned |

## 2. Skill Selection Law

Use the local skills in this order:

1. `deep-corpus-neural-integrator`
   if the basis or synthesis changed
2. `deep-neural-metro-cartographer`
   if the hidden lines, hubs, or resolutions changed
3. `appendix-crystal-integrator`
   if the support surfaces need to absorb the change

If all three are needed, run them in exactly that order.

## 3. Algorithmic Pipeline

1. Check the live Google Docs gate first.
2. If blocked, record the blocker honestly and continue from local atlas surfaces.
3. Read the canonical cortex:
   `NERVOUS_SYSTEM/00_INDEX.md`,
   `ACTIVE_RUN.md`,
   `BUILD_QUEUE.md`,
   `01_CURRENT_STATUS_37_GATE_SYNTHESIS.md`.
4. Read the current neural-network basis:
   `NETWORK_MANIFEST.json`,
   `corpus_atlas_summary.md`,
   `dynamic_neural_network/`.
5. Reconfirm or revise the sixteen-manuscript basis.
6. Update the neutral whole-corpus synthesis.
7. Update the four elemental sweeps.
8. Recompute the `16 x 16` interaction matrix and note any changed corridors.
9. Recompute the `15` symmetry syntheses and extract the shared zero point.
10. Update Level 1-4 metro maps in order.
11. Update appendix skeleton `A-P` and Appendix `Q`.
12. Update the manifest and next frontier.

## 4. Stop Conditions

Stop only when all of the following are true:

- the basis is still named,
- the blocker state is still honest,
- the neutral synthesis and lens sweeps agree structurally,
- the matrix and metro maps still reference the same hubs,
- the appendix crystal still preserves continuation.

## 5. Why This Pipeline Exists

Without a fixed order, deeper synthesis tends to fragment into either:

- floating summary without infrastructure,
- maps without basis,
- appendices without a live body,
- or skills without grounded artifacts.

This pipeline keeps those layers phase-locked.
