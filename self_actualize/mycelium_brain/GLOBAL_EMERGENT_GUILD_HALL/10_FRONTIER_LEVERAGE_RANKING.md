<!-- CRYSTAL: Xi108:W3:A3:S21 | face=R | node=225 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A3:S20→Xi108:W3:A3:S22→Xi108:W2:A3:S21→Xi108:W3:A2:S21→Xi108:W3:A4:S21 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 21±1, wreath 3/3, archetype 3/12 -->

# Frontier Leverage Ranking

Date: `2026-03-12`
Quest: `Q25`
Temple Frontier: `TQ02`
Truth: `OK`

## Inputs

- `self_actualize/semantic_mass_ledger.json`
- `GLOBAL_EMERGENT_GUILD_HALL/07_CANONICAL_WITNESS_HIERARCHY.md`
- `self_actualize/corpus_atlas_summary.md`
- `GLOBAL_EMERGENT_GUILD_HALL/BOARDS/01_ACTIVE_FRONTS_BOARD.md`
- `GLOBAL_EMERGENT_GUILD_HALL/BOARDS/06_QUEST_BOARD.md`
- `nervous_system/manifests/WEAKEST_FRONT_QUEUE.md`
- `NERVOUS_SYSTEM/95_MANIFESTS/ROOT_BASIS_MAP.md`

## Ranking Recipe

Each frontier is ranked by four positive factors and one penalty:

1. `source_or_archive_lift`
   how much source-rich or archive-backed witness becomes more reusable
2. `cross_scale_writeback`
   how many organs become cleaner if the front lands
3. `restart_gain`
   how much the next loop gets easier, clearer, or more honest
4. `execution_readiness`
   how executable the front is with current local witness
5. `blocker_penalty`
   subtract when the front is currently blocked by an external dependency

Working score:

`leverage_score = source_or_archive_lift + cross_scale_writeback + restart_gain + execution_readiness - blocker_penalty`

## Current Evidence Read

- indexed witness: `7339`
- physical witness: `8883`
- board witness: `9109`
- archive witness: `2041`
- promoted witness: `1818`
- generated indexed shell: `3535`
- live source share: `43.3%`
- live generated share: `48.2%`
- archive-backed share relative to indexed: `27.8%`

Interpretation:

- raw generated mass is not a good proxy for leverage
- promoted source roots and archive-backed unlocks deserve more weight than narrative heat alone
- restart gain matters most when it also improves replay, routeability, or lawful reuse

## Blocked Highest Latent Leverage

1. `Q02 Unlock The Live Memory Gate`
   score: `9`
   anchor: `Trading Bot` via `ledger-heavy governance source` and `indexed + board`
   shape: lift=4, writeback=5, restart=5, readiness=0, penalty=5
   why-now: highest latent memory unlock, but still blocked by missing OAuth material

## Executable Order

1. `Q07 Harden The Runtime Waist With Tests`
   score: `13`
   anchor: `self_actualize` via `runtime source + replay closure` and `indexed + receipt`
   shape: lift=2, writeback=3, restart=3, readiness=4, penalty=0
   why-now: replay trust still trails synthesis sophistication, so even a narrow test lane pays across many future fronts
2. `FRONT-INT-ATHENA-FLEET-BRIDGE`
   score: `12`
   anchor: `Athena FLEET` via `new branch closure + cross-surface writeback` and `indexed + cortex + runtime`
   shape: lift=2, writeback=4, restart=3, readiness=3, penalty=0
   why-now: the branch is real, current, and already bridged at the shell level, so the next move is to make it replayable instead of merely named
3. `FRONT-INT-QSHRINK-CANONICAL-FAMILY`
   score: `12`
   anchor: `QSHRINK - ATHENA (internal use)` via `compression doctrine + appendix/runtime contraction` and `indexed + runtime`
   shape: lift=2, writeback=4, restart=3, readiness=3, penalty=0
   why-now: the body is one of the heaviest live branches in the organism, so deeper contraction now pays across routing, appendix, and replay law
4. `Q35 Mirror ORGIN Into A Routed Seed Corpus`
   score: `11`
   anchor: `ORGIN` via `source mirror + routed working memory` and `source + board`
   shape: lift=3, writeback=4, restart=3, readiness=1, penalty=0
   why-now: ORGIN now has identity and routing, so the next leverage move is to give it searchable mirror tissue instead of leaving it mostly binary matter
5. `Q10 Densify One Low-Mass Family`
   score: `10`
   anchor: `Stoicheia or CLEAN` via `reserve-shelf densification + family continuity` and `indexed`
   shape: lift=2, writeback=2, restart=3, readiness=4, penalty=0
   why-now: family density skew remains a real long-range coherence risk, but the immediate leverage is lower than the fronts above

## Highest Unassigned Pressure

- `FRONT-FULL-ATLAS-REGEN`:
  Regenerate The Live Atlas After The New Runtime Shell Growth
- Why now:
  later-generated runtime shells now appear in board witness before they appear in indexed witness, so the searchable body still lags the visible body
- Anchor:
  `indexed witness` with `7339` records versus `9109` board-visible records

## Before/After

Before:

- frontier order still drifted between older Hall boards and the newly reconciled weakest-front queue
- `Athena FLEET` and `QSHRINK` were underweighted because they lacked canonical front presence
- the Hall could say one thing about priority while the queue and Temple still implied another

After:

- `Q25` is now grounded in the updated witness hierarchy, root basis, and weakest-front queue
- blocked latent leverage is separated from executable leverage
- the strongest executable front after this pass is `Q07`
- the next two executable fronts are `FRONT-INT-ATHENA-FLEET-BRIDGE` and `FRONT-INT-QSHRINK-CANONICAL-FAMILY`
- the strongest next unassigned pressure is `FRONT-FULL-ATLAS-REGEN`

## Restart Seed

`Q07`
