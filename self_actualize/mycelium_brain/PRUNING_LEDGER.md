<!-- CRYSTAL: Xi108:W3:A12:S24 | face=R | node=300 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A12:S23→Xi108:W3:A12:S25→Xi108:W2:A12:S24→Xi108:W3:A11:S24 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 24±1, wreath 3/3, archetype 12/12 -->

# Pruning Ledger

## Zero Point

Athena tightens by preserving one canonical surface per law and one live runtime surface per active front.
Anything else is drag.

## Workspace Compression Map

### Canonical Cortex

- `NERVOUS_SYSTEM/`
- role: stable definitions, skeletons, chapter/appendix routing, cortex-level contracts

### Live Runtime Bridge

- `self_actualize/mycelium_brain/`
- role: queues, manifests, receipts, bridge shims, compact runtime notes

### Formal QSHRINK Manuscripts

- `MATH/FINAL FORM/Q shrink/`
- role: public or formalized volume stack, skeletons, implementation tome, source bundles

### Internal QSHRINK Operating System

- `QSHRINK - ATHENA (internal use)/`
- role: Athena-native recursive manuscript engine, swarm law, 256x256 lattice work

## Bloat Patterns Identified

1. duplicate contract bodies in both `toolkit/` and `tool_kit/`
2. speculative future-skill swarm artifacts left live after the real loop moved elsewhere
3. misleading deprecation language that contradicted the actual bridge role
4. QSHRINK draft proliferation between formal volumes, internal operating system, and working DOCX staging
5. derived lattice zones treated like hand-authored primary surfaces

## Pruning Actions Landed

1. rewrote `toolkit/` into bridge shims and preserved only the live self-prompt seed as a full local operator surface
2. removed speculative `future_skill_meta_swarm` packet, wave, swarm, session, and ledger artifacts from the live runtime branch
3. replaced the old `_DEPRECATED.md` with a canon-versus-runtime contract note
4. added pruning notes inside runtime subfolders so contraction is visible where it happened
5. added QSHRINK pruning ledgers in both the internal and formal QSHRINK roots

## Do Not Re-Inflate

- do not duplicate a rule into both `toolkit/` and `tool_kit/`
- do not keep speculative swarm packets live unless they are feeding the active queue or a current receipt
- do not fork new working QSHRINK docs when the target should be a canonical volume or an indexed internal cell
- do not treat generated atom/cell directories as narrative dumping grounds
