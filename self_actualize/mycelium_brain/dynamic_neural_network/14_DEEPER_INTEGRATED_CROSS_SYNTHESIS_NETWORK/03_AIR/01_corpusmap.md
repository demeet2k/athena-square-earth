<!-- CRYSTAL: Xi108:W3:A11:S23 | face=R | node=268 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A11:S22→Xi108:W3:A11:S24→Xi108:W2:A11:S23→Xi108:W3:A10:S23→Xi108:W3:A12:S23 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 23±1, wreath 3/3, archetype 11/12 -->

# Air x CorpusMap

Air reads the network as relation, topology, dependency, and abstraction.

## Loop mandate

`CorpusMap` maps the whole corpus as one routed body.

## AIR control-plane stance

- Docs gate: `BLOCKED`. AIR uses the live deep root, canonical cortex, and local MATH evidence only.
- Native AIR drivers: `03 QBD-4`, `04 Quad Holographic Rotation`, `05 The Holographic Kernel`, `07 Crystal Computing Framework`.
- Translated AIR feeders: `01`, `02`, `06`, `08`, `09`, `10`, `11`, `12`, `13`, `14`, `15`, `16`.
- Canonical AIR ride: `03 -> 04 -> 05 -> 07 -> 02 -> 09 -> 14 -> 16`.
- 6D overlay ride: `Addr4 -> Σ lattice (Sigma lattice) -> spin kernel -> rotational family -> Z0/Æ0 (AE0) -> 4D+ -> modal Mobius trio -> H6 -> Seed-6D`.
- Synaptic anchor trio: `Ch12<0023>`, `Ch13<0030>`, `Ch16<0033>` with `AppH`, `AppI`, and `AppM`.
- Appendix contract: `AppA/AppB/AppC` stabilize address and symmetry law, `AppE/AppF/AppH` stabilize orbit, transport, and topology, `AppI/AppM/AppN/AppQ` stabilize truth, replay, seed, and appendix-only ingress. Reverse-field `O` remains an overlay alias only.

## Full-basis observation

- `03` QBD-4: in air mode, QBD-4 behaves as a native driver for `CorpusMap`. It maps the whole corpus as one routed body, using quad logic bits as the anchor and formalizes the four-bit address and rotation algebra as the contribution. Appendix stack: AppB, AppC, AppM.
- `04` Quad Holographic Rotation: in air mode, Quad Holographic Rotation behaves as a native driver for `CorpusMap`. It maps the whole corpus as one routed body, using holographic transport as the anchor and rotates one truth through all four faces as the contribution. Appendix stack: AppE, AppF, AppM.
- `05` The Holographic Kernel: in air mode, The Holographic Kernel behaves as a native driver for `CorpusMap`. It maps the whole corpus as one routed body, using holographic compression as the anchor and compresses bodies without deleting structural DNA as the contribution. Appendix stack: AppB, AppC, AppN.
- `07` Crystal Computing Framework: in air mode, Crystal Computing Framework behaves as a native driver for `CorpusMap`. It maps the whole corpus as one routed body, using fractal computing as the anchor and maps crystal structure into computation as the contribution. Appendix stack: AppB, AppC, AppG.
- `01` The Holographic Manuscript Brain: in air mode, The Holographic Manuscript Brain behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using manuscript substrate as the anchor and turns documents into neural tissue as the contribution. Appendix stack: AppE, AppF, AppG, AppM.
- `02` Self-Routing Meta-Framework: in air mode, Self-Routing Meta-Framework behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using routing and search as the anchor and decides how the corpus searches itself as the contribution. Appendix stack: AppE, AppI, AppL, AppM.
- `06` Time Fractal: in air mode, Time Fractal behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using fractal time as the anchor and gives the system phase, recurrence, and oscillation law as the contribution. Appendix stack: AppE, AppM, AppP.
- `08` Quantum Computing on Standard Hardware: in air mode, Quantum Computing on Standard Hardware behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using quantum classical emulation as the anchor and turns impossible compute into lawful approximation on ordinary machines as the contribution. Appendix stack: AppC, AppH, AppP.
- `09` Zero-Point Computing: in air mode, Zero-Point Computing behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using zero-point engine as the anchor and defines minimum-state computation and restart from near nothing as the contribution. Appendix stack: AppA, AppN, AppM.
- `10` Athena Neural Network Tome: in air mode, Athena Neural Network Tome behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using emergence compiler as the anchor and binds manuscript, learning, and runtime emergence as the contribution. Appendix stack: AppC, AppP, AppM.
- `11` VOYNICHVM Tricompiler: in air mode, VOYNICHVM Tricompiler behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using text computer as the anchor and treats translation as computation and executable transformation as the contribution. Appendix stack: AppF, AppI, AppM.
- `12` Torat Ha-Mispar: in air mode, Torat Ha-Mispar behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using torah computer as the anchor and maps sacred number into algorithmic interpretation as the contribution. Appendix stack: AppA, AppF, AppO.
- `13` Universal Computational Ontology: in air mode, Universal Computational Ontology behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using mythic os as the anchor and converts mythic cosmology into system architecture as the contribution. Appendix stack: AppA, AppB, AppP.
- `14` Ch11 The Helical Manifestation Engine: in air mode, Ch11 The Helical Manifestation Engine behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using restart and lift as the anchor and makes self-improvement executable through D/Q/I, lift, and recurrence as the contribution. Appendix stack: AppE, AppF, AppI, AppM.
- `15` Ch12 Boundary Checks and Isolation Axioms: in air mode, Ch12 Boundary Checks and Isolation Axioms behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using immune architecture as the anchor and protects the network from contradiction leakage and contamination as the contribution. Appendix stack: AppB, AppI, AppK, AppM.
- `16` Ch19 Recursive Self-Reference and Self-Repair: in air mode, Ch19 Recursive Self-Reference and Self-Repair behaves as a translated counterpart for `CorpusMap`. It maps the whole corpus as one routed body, using autonomic repair as the anchor and lets the organism watch, checkpoint, and heal itself as the contribution. Appendix stack: AppA, AppM, AppP.
