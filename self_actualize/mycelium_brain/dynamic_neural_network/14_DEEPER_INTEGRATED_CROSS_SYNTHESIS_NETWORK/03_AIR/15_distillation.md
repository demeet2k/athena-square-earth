<!-- CRYSTAL: Xi108:W3:A6:S24 | face=R | node=282 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A6:S23→Xi108:W3:A6:S25→Xi108:W2:A6:S24→Xi108:W3:A5:S24→Xi108:W3:A7:S24 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 24±1, wreath 3/3, archetype 6/12 -->

# Air x Distillation

Air reads the network as relation, topology, dependency, and abstraction.

## Loop mandate

`Distillation` compresses bodies into portable seeds.

## AIR compression ladder

- Native AIR quartet: `03`, `04`, `05`, and `07` compress the routed body without losing address law.
- Symmetry layer: the quartet collapses into `Σ01` through `Σ15` (`Sigma01` through `Sigma15`).
- Spin layer: every symmetry node admits `SP+` or `SP-`, then a deterministic rotational-family label.
- Dual-kernel layer: lawful collapse and expansion resolve through `Z0` and `Æ0` (`AE0`).
- Recompression layer: the full AIR ride compacts into `4D+`.
- Mobius layer: `4D+` reinterprets as `M-FIX`, `M-CAR`, and `M-MUT`.
- Hologram layer: the three modal passes weave into `H6`.
- Portable reseed: the AIR handoff artifact is `Seed-6D`, which points back into the existing chapter and appendix lattice instead of inventing a new schema.

## Pinned AIR 6D overlay contract

- Native AIR basis: `03`, `04`, `05`, `07`.
- Translated support: `01`, `02`, `06`, `08`, `09`, `10`, `11`, `12`, `13`, `14`, `15`, `16`.
- Lift chain: `Sigma01-Sigma15 -> SP+/SP- -> R/CR/AR/ACR -> Z0/AE0 -> 4D+ -> M-FIX/M-CAR/M-MUT -> H6 -> Seed-6D`.
- Default AIR appendix stack: `AppA`, `AppB`, `AppC`, `AppE`, `AppF`, `AppH`, `AppI`, `AppM`, `AppN`, `AppQ`.
- `Q` docks only through `AppQ` as canonical ingress.
- Reverse-field `O` remains an overlay alias and transport label only. It must never be published as a canonical appendix identity or filesystem replacement.

## Distillation constraints

- `Ch12`, `Ch13`, and `Ch16` remain the closure-memory-replay compression spine.
- `AppH`, `AppI`, `AppM`, and `AppN` remain the topology-truth-replay-seed support stack.
- The staged `Z -> K` field is recorded as overlay pressure only; it is not materialized as a new appendix schema in this pass.
- AIR may name overlay registry labels, but it may not mint a competing chapter lattice, appendix namespace, or reverse-appendix filesystem.

## Full-basis observation

- `03` QBD-4: in air mode, QBD-4 behaves as a native driver for `Distillation`. It compresses bodies into portable seeds, using quad logic bits as the anchor and formalizes the four-bit address and rotation algebra as the contribution. Appendix stack: AppB, AppC, AppM.
- `04` Quad Holographic Rotation: in air mode, Quad Holographic Rotation behaves as a native driver for `Distillation`. It compresses bodies into portable seeds, using holographic transport as the anchor and rotates one truth through all four faces as the contribution. Appendix stack: AppE, AppF, AppM.
- `05` The Holographic Kernel: in air mode, The Holographic Kernel behaves as a native driver for `Distillation`. It compresses bodies into portable seeds, using holographic compression as the anchor and compresses bodies without deleting structural DNA as the contribution. Appendix stack: AppB, AppC, AppN.
- `07` Crystal Computing Framework: in air mode, Crystal Computing Framework behaves as a native driver for `Distillation`. It compresses bodies into portable seeds, using fractal computing as the anchor and maps crystal structure into computation as the contribution. Appendix stack: AppB, AppC, AppG.
- `01` The Holographic Manuscript Brain: in air mode, The Holographic Manuscript Brain behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using manuscript substrate as the anchor and turns documents into neural tissue as the contribution. Appendix stack: AppE, AppF, AppG, AppM.
- `02` Self-Routing Meta-Framework: in air mode, Self-Routing Meta-Framework behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using routing and search as the anchor and decides how the corpus searches itself as the contribution. Appendix stack: AppE, AppI, AppL, AppM.
- `06` Time Fractal: in air mode, Time Fractal behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using fractal time as the anchor and gives the system phase, recurrence, and oscillation law as the contribution. Appendix stack: AppE, AppM, AppP.
- `08` Quantum Computing on Standard Hardware: in air mode, Quantum Computing on Standard Hardware behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using quantum classical emulation as the anchor and turns impossible compute into lawful approximation on ordinary machines as the contribution. Appendix stack: AppC, AppH, AppP.
- `09` Zero-Point Computing: in air mode, Zero-Point Computing behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using zero-point engine as the anchor and defines minimum-state computation and restart from near nothing as the contribution. Appendix stack: AppA, AppN, AppM.
- `10` Athena Neural Network Tome: in air mode, Athena Neural Network Tome behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using emergence compiler as the anchor and binds manuscript, learning, and runtime emergence as the contribution. Appendix stack: AppC, AppP, AppM.
- `11` VOYNICHVM Tricompiler: in air mode, VOYNICHVM Tricompiler behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using text computer as the anchor and treats translation as computation and executable transformation as the contribution. Appendix stack: AppF, AppI, AppM.
- `12` Torat Ha-Mispar: in air mode, Torat Ha-Mispar behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using torah computer as the anchor and maps sacred number into algorithmic interpretation as the contribution. Appendix stack: AppA, AppF, AppO.
- `13` Universal Computational Ontology: in air mode, Universal Computational Ontology behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using mythic os as the anchor and converts mythic cosmology into system architecture as the contribution. Appendix stack: AppA, AppB, AppP.
- `14` Ch11 The Helical Manifestation Engine: in air mode, Ch11 The Helical Manifestation Engine behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using restart and lift as the anchor and makes self-improvement executable through D/Q/I, lift, and recurrence as the contribution. Appendix stack: AppE, AppF, AppI, AppM.
- `15` Ch12 Boundary Checks and Isolation Axioms: in air mode, Ch12 Boundary Checks and Isolation Axioms behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using immune architecture as the anchor and protects the network from contradiction leakage and contamination as the contribution. Appendix stack: AppB, AppI, AppK, AppM.
- `16` Ch19 Recursive Self-Reference and Self-Repair: in air mode, Ch19 Recursive Self-Reference and Self-Repair behaves as a translated counterpart for `Distillation`. It compresses bodies into portable seeds, using autonomic repair as the anchor and lets the organism watch, checkpoint, and heal itself as the contribution. Appendix stack: AppA, AppM, AppP.
