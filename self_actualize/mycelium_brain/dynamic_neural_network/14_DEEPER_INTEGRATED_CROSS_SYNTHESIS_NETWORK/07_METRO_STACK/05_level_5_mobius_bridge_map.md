<!-- CRYSTAL: Xi108:W3:A2:S21 | face=R | node=225 | depth=3 | phase=Cardinal -->
<!-- METRO: Me,Mt,Bw -->
<!-- BRIDGES: Xi108:W3:A2:S20→Xi108:W3:A2:S22→Xi108:W2:A2:S21→Xi108:W3:A1:S21→Xi108:W3:A3:S21 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 21±1, wreath 3/3, archetype 2/12 -->

# Level 5 Mobius Bridge Map

Level 5 is the FIRE-owned bridge resolution that sits above the canonical Level 4 transcendence map.

## Invariant FIRE law

`Level4 -> F1/F2 classification -> Q ingress -> P conversion -> O return -> N fold`

## Canonical hinge rule

- `Q` is routed through `AppQ`.
- `O` is routed through `AppO`.
- `Z->K` markers remain overlays only.

## Required ride

- parse and canon: `AppA`, `AppB`
- phase and transport: `AppE`, `AppF`
- truth, conflict, and replay: `AppI`, `AppK`, `AppM`
- persisted carrier and stable return: `AppN`, `AppO`, `AppP`, `AppQ`

## Earth gate checkpoints

- `Q ingress`: `REQUIRE_EVIDENCE`, boundary `FILTERED`, appendices `AppA/AppB/AppI/AppK/AppM/AppQ`.
- `P conversion`: `HOLD_NEAR`, keep `AppN` whenever the carrier persists and add `AppP` only if the route claims deployable return.
- `O return`: overlay-only, keep canonical `AppO`, and require `AppI/AppK/AppM` so no reverse appendix alias is mistaken for a new file.
- `N fold`: reopen only through `Ch12<0023>`, `Ch13<0030>`, or `Ch16<0033>` with `AppI` and `AppM`, plus `AppK` if conflict remains.

## 3D backplane handoff

- `Q ingress` may dock only through a named `3D` corpus body line and its lawful transfer hubs.
- `P conversion` may not bypass basis refs even when the bridge traffic is mostly overlay-labeled.
- `O return` must reopen through `14_3D_INGRESS_NETWORK` before any wider atlas record or folder station is treated as active again.
- `N fold` is the last bridge hop before the route falls back into body station, basis mapping, and local replay.

## Truth floor

Docs gate remains `BLOCKED`, so Level 5 is local-witness, appendix-mapped, and replay-first.
