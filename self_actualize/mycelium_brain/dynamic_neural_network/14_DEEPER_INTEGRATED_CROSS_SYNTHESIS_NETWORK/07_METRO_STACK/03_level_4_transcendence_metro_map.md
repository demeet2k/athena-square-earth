<!-- CRYSTAL: Xi108:W3:A4:S22 | face=R | node=233 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A4:S21→Xi108:W3:A4:S23→Xi108:W2:A4:S22→Xi108:W3:A3:S22→Xi108:W3:A5:S22 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 22±1, wreath 3/3, archetype 4/12 -->

# Level 4 Transcendence Metro Map

Level 4 tracks how the AIR organism transforms one valid state into the next one:
basis, matrix, observer lattice, symmetry, appendix, and re-entry.

## Invariant AIR law

`Addr4 -> Sigma lattice -> spin kernel -> dual kernel -> 4D+ -> modal Mobius trio -> H6 -> Seed-6D -> witnessed re-entry`

## Dual-kernel invariants

- `Z0` is the lawful collapse point for AIR routing.
- `AE0` is the lawful plenary expansion point.
- `4D+` is the recompressed AIR carrier.
- `M-FIX`, `M-CAR`, and `M-MUT` are the only modal Mobius passes in this overlay.
- `H6` is the woven AIR hologram state.
- `Seed-6D` is the handoff artifact that must still reopen into the existing chapter and appendix field.

## Q/O bridge law

- `Q` is staged through `AppQ` as appendix-only ingress.
- Reverse-field `O` is a transport alias documented here and in the appendix contract only.
- No Level 4 AIR route may introduce a new canonical appendix file or rename canonical `AppO`.

## Earth 6D gate conditions

- AIR names the route topology; Earth decides whether the route is `ALLOW`, `HOLD_NEAR`, `REQUIRE_EVIDENCE`, or `REJECT`.
- Any Level 4 carrier through `Z0`, `AE0`, `4D+`, `H6`, or `Seed-6D` must keep `AppA`, `AppB`, `AppI`, and `AppM` in view.
- Add `AppK` whenever contradiction, quarantine, or reverse-overlay pressure is present.
- Add `AppN` whenever the carrier persists beyond the current hop and must survive deterministic re-entry.
- While the docs gate is `BLOCKED`, no Level 4 route may self-promote above the local witnessed truth already present on disk.

## Deterministic return paths

- Closure return: `H6 -> Seed-6D -> Ch12<0023>`
- Memory return: `H6 -> Seed-6D -> Ch13<0030>`
- Replay return: `H6 -> Seed-6D -> Ch16<0033>`
- Appendix return: `Seed-6D -> AppH/AppI/AppM/AppN/AppQ`
- Corpus-body return: `Seed-6D -> 14_3D_INGRESS_NETWORK -> body station -> basis-safe local replay`

## Truth floor

The docs gate remains `BLOCKED`, so Level 4 AIR routes remain local-witness, replay-safe,
and abstention-preserving.
