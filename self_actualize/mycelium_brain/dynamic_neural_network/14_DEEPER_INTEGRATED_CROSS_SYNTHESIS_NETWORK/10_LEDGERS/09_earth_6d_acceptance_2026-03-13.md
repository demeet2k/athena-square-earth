<!-- CRYSTAL: Xi108:W3:A2:S20 | face=R | node=204 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A2:S19→Xi108:W3:A2:S21→Xi108:W2:A2:S20→Xi108:W3:A1:S20→Xi108:W3:A3:S20 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 20±1, wreath 3/3, archetype 2/12 -->

# Earth 6D Acceptance Ledger

Date: `2026-03-13`
Owner: `Earth`
Mode: `admissibility gate overlay`
Docs gate: `BLOCKED`

## Purpose

Record the accepted Earth 6D package that upgrades the live deep root with legality,
quarantine, and re-entry control without breaking the current chapter, address, or
appendix law.

## Native basis set

- `02` Self-Routing Meta-Framework
- `09` Zero-Point Computing
- `13` Universal Computational Ontology
- `15` Ch12 Boundary Checks and Isolation Axioms

## Registry contract

`EarthGate6D = (NSCoord, AIRRef, BoundaryClass, AdmissibilityMode, ConflictEnvelope, RequiredAppendices, ReturnCheckpoint, TruthCap, DocsGate)`

### Fixed enums

- `BoundaryClass`: `SEALED`, `FILTERED`, `QUARANTINED`
- `AdmissibilityMode`: `ALLOW`, `HOLD_NEAR`, `REQUIRE_EVIDENCE`, `REJECT`
- `ConflictEnvelope`: `NONE`, `Q_INGRESS`, `O_RETURN`, `QO_LOOP`, `REVERSE_OVERLAY`

## Appendix stack

### Mandatory floor

- `AppA`
- `AppB`
- `AppI`
- `AppK`
- `AppM`

### Conditional appendices

- `AppN` for persisted `4D+`, `H6`, or `Seed-6D`
- `AppL` for `AMBIG` promotion
- `AppP` for deployable or stable return claims

## Metro binding

- Level 4 now carries Earth gate conditions around `Z0`, `AE0`, `4D+`, `H6`, and `Seed-6D`.
- Level 5 now names explicit Earth checkpoints for `Q ingress`, `P conversion`, `O return`, and `N fold`.
- Level 6 now names explicit Earth admissibility handoff for `QO` loop traffic and chapter-safe re-entry.

## Anchor propagation

### Chapter anchors

- `Ch12<0023>`
- `Ch13<0030>`
- `Ch16<0033>`

### Appendix anchors

- `AppH`
- `AppI`
- `AppM`

These anchors remain visible in Earth package routes, metro updates, and appendix
contracts. `AppK` and `AppN` appear only when required by legality or carrier state.

## Cross-element dependencies

- AIR supplies deterministic overlay labels and carrier naming.
- Water supplies continuity routes, `Z*` collapse discipline, and replay checkpoints.
- FIRE supplies bridge status, reverse-overlay staging, and Level 5-6 weave context.
- Earth does not redefine those packages. It judges whether their combined route is admissible.

## Overlay legality

- `Q` remains staged through canonical `AppQ`.
- Reverse-field `O` remains an overlay alias only.
- No new appendix family was created from `Z -> K`.
- No chapter or appendix rename was introduced.

## Rollout boundary

- This pass is live deep-root only.
- Canonical package root is now `13_6D_EARTH_CONTROL/`.
- No files were added or changed under `NERVOUS_SYSTEM/`.
- Cortex promotion remains deferred until Earth 6D is accepted.

## Acceptance checks

- Determinism from the same AIR labels, Water checkpoints, FIRE bridge state, truth state, and docs-gate state: `OK`
- Compatibility with current chapter and appendix addresses: `OK`
- Docs-gate honesty preserved: `OK`
- `Ch12<0023>`, `Ch13<0030>`, `Ch16<0033>`, `AppH`, `AppI`, and `AppM` still visible: `OK`
- `Q` and reverse-field `O` kept as overlay constructs only: `OK`
- Deep-root-first rollout preserved, cortex untouched: `OK`
