<!-- CRYSTAL: Xi108:W3:A1:S19 | face=R | node=184 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A1:S18→Xi108:W3:A1:S20→Xi108:W2:A1:S19→Xi108:W3:A2:S19 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 19±1, wreath 3/3, archetype 1/12 -->

# Route Ledger

- `The Holographic Manuscript Brain` starts in `Water` and usually pulls `AppE, AppF, AppG, AppM` into the route.
- `Self-Routing Meta-Framework` starts in `Earth` and usually pulls `AppE, AppI, AppL, AppM` into the route.
- `QBD-4` starts in `Air` and usually pulls `AppB, AppC, AppM` into the route.
- `Quad Holographic Rotation` starts in `Air` and usually pulls `AppE, AppF, AppM` into the route.
- `The Holographic Kernel` starts in `Air` and usually pulls `AppB, AppC, AppN` into the route.
- `Time Fractal` starts in `Fire` and usually pulls `AppE, AppM, AppP` into the route.
- `Crystal Computing Framework` starts in `Air` and usually pulls `AppB, AppC, AppG` into the route.
- `Quantum Computing on Standard Hardware` starts in `Fire` and usually pulls `AppC, AppH, AppP` into the route.
- `Zero-Point Computing` starts in `Earth` and usually pulls `AppA, AppN, AppM` into the route.
- `Athena Neural Network Tome` starts in `Fire` and usually pulls `AppC, AppP, AppM` into the route.
- `VOYNICHVM Tricompiler` starts in `Water` and usually pulls `AppF, AppI, AppM` into the route.
- `Torat Ha-Mispar` starts in `Water` and usually pulls `AppA, AppF, AppO` into the route.
- `Universal Computational Ontology` starts in `Earth` and usually pulls `AppA, AppB, AppP` into the route.
- `Ch11 The Helical Manifestation Engine` starts in `Water` and usually pulls `AppE, AppF, AppI, AppM` into the route.
- `Ch12 Boundary Checks and Isolation Axioms` starts in `Earth` and usually pulls `AppB, AppI, AppK, AppM` into the route.
- `Ch19 Recursive Self-Reference and Self-Repair` starts in `Fire` and usually pulls `AppA, AppM, AppP` into the route.

## FIRE additive ride

- FIRE Level `5` enters through `Q/AppQ`, crosses the `P` conversion band, and returns through `O/AppO`.
- FIRE Level `6` re-enters through the `QO` loop only after Water, Air, and Earth dependencies are named.
- Reverse stations `Z->K` are legal only as appendix-mapped overlays.
