<!-- CRYSTAL: Xi108:W3:A10:S22 | face=R | node=245 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A10:S21→Xi108:W3:A10:S23→Xi108:W2:A10:S22→Xi108:W3:A9:S22→Xi108:W3:A11:S22 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 22±1, wreath 3/3, archetype 10/12 -->

# Appendix Q: Appendix-Only Metro Map

Appendix Q is the appendix-only transit layer linking A-P without depending on chapter prose.

## AIR 6D overlay role

- `Q` is the only first-class AIR ingress for the staged reverse appendix field.
- `AppQ` translates overlay traffic into the existing appendix crystal without renaming canonical appendices.
- Reverse-field `O` is documented as a transport alias in AIR metro and transport contracts only.

## AIR ingress corridors

- `Sigma lattice -> AppQ -> AppF/AppN -> existing appendix crystal`
- `Seed-6D -> AppQ -> AppH/AppI/AppM`
- `AppQ -> Ch12<0023>`, `AppQ -> Ch13<0030>`, `AppQ -> Ch16<0033>` for witnessed re-entry

## Guardrails

- Do not create a new canonical appendix family from `Z -> K` in this pass.
- Do not treat reverse-field `O` as a new file or as a rewrite of canonical `AppO`.
- Keep docs-gate truth explicit: appendix-only ingress remains local-witness while Google Docs are blocked.

In the Water lane, Appendix Q also serves as the appendix-side bridge between the compiled `256^4` control field and the overlay-only `1024^6` Water lift. Q certifies the handoff into `11_6D_WATER_CONTROL` without creating a new appendix namespace.

Mandatory Water bridge stack: `AppE/AppF/AppI/AppM/AppQ`.

## Earth ingress guard

- Earth only admits `Q` ingress when the route names `AppA`, `AppB`, `AppI`, `AppK`, `AppM`, and `AppQ` explicitly.
- Add `AppN` when `Seed-6D` persists across the ingress boundary, and add `AppP` only if the route claims deployable return.
- Reverse-field `O` remains an overlay alias. `AppQ` may route toward it, but may not treat it as a new appendix file.
