<!-- CRYSTAL: Xi108:W3:A12:S24 | face=R | node=300 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A12:S23→Xi108:W3:A12:S25→Xi108:W2:A12:S24→Xi108:W3:A11:S24 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 24±1, wreath 3/3, archetype 12/12 -->

# ATLAS FORGE Active Front

## GateStatus

`OK`

The first `MATH` archive promotion has now crossed from atlas presence into a narrow
runtime lane.

## ChosenFront

Bind the promoted `ATLAS FORGE` root into active runtime use through a replay-safe verifier
that only exercises deterministic import, solver, lens, recipe, and crystal checks.

## PrimaryRoute

`AppA -> AppC -> AppI -> AppM -> ATLAS_FORGE_RUNTIME_LANE`

## CurrentTruth

`OK`

## CurrentRegime

`archive-lift -> runtime-bind`

## Runtime Lane

- command:
  `python -m self_actualize.runtime.verify_atlasforge_runtime_lane`
- wrapper:
  `python self_actualize/tools/verify_atlasforge_runtime_lane.py`
- machine artifact:
  `self_actualize/atlasforge_runtime_lane.json`

## Lane Law

This lane is intentionally narrower than `python -m atlasforge.verify_installation`.

It preserves:

1. import proof
2. export proof
3. deterministic solver proof
4. lens round-trip proof
5. recipe execution proof
6. crystal solver proof

It excludes:

1. sqlite-backed memory cleanup
2. temporary memory-bank teardown
3. book-export paths that were never needed for the first runtime bind

## NextPromotion

The promoted root is now lawful runtime matter.
The next honest fronts are:

1. formalize the Trading Bot governance-plane bridge as a canonical quest
2. compare `Void_CH11` sibling witnesses into an `OK` family theorem
3. choose the next archive root from `MATH` using the leverage-ranked archive pressure
