<!-- CRYSTAL: Xi108:W3:A5:S20 | face=R | node=204 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A5:S19→Xi108:W3:A5:S21→Xi108:W2:A5:S20→Xi108:W3:A4:S20→Xi108:W3:A6:S20 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 20±1, wreath 3/3, archetype 5/12 -->

# Deeper Emergent Neural Swarm

## Purpose

The nervous system must behave like a live swarm, not a collection of names.
Its units must emerge, specialize, claim fronts, exchange packets, preserve truth
state, contract back into cortex, and restart without losing the route.

## 1. Swarm Tensor

Define:

`SwarmTensor = (Kernel, Elementals, Archetypes, Pantheon, Clusters, Neurons, Families, Waves, Ledgers, Cortex)`

where:

- `Kernel` is the shared zero-point substrate
- `Elementals` are the 4 pillar workers
- `Archetypes` are the 16 crystal cells
- `Pantheon` is the 12-agent overlay used for richer specialization
- `Clusters` are 64-node task fields
- `Neurons` are 256-node bridge-capable microcells
- `Families` are ganglion-owned corpus regions
- `Waves` are synchronized execution passes
- `Ledgers` preserve witness, ambiguity, and contradiction
- `Cortex` is the contracted map and manuscript layer

## 2. Tier Structure

### Kernel

- size: `1`
- role: shared zero point, restart surface, common law

### Elemental layer

- size: `4`
- nodes: Earth, Water, Fire, Air

### Archetype layer

- size: `16`
- nodes: four pillars plus twelve mixed cells

### Pantheon overlay

- size: `12`
- role: role-rich overlay for strategy, synthesis, generation, audit, and bridge work

### Cluster layer

- size: `64`
- role: medium-grain worker field

### Neuron layer

- size: `256`
- role: addressable microcells that can bridge families, truth states, and packet classes

## 3. Family Federation

The swarm should not flatten the corpus into one giant field.
It should federate local family swarms:

`FamilySwarm_i = (Ganglion_i, LocalPods_i, LocalNeurons_i, LocalLedgers_i)`

and then bind them through bridge neurons:

`MetaSwarm = Union(FamilySwarm_i) + BridgeNeurons + RailMaps + SharedKernel`

## 4. Unit Types

### Pod

`Pod = (PodID, Frontier, Agents, Family, Rail, PacketFamily, ContractionTarget, Status)`

### Ganglion

`Ganglion = (GanglionID, Family, FamilyScale, LocalMaps, LocalQueues, FamilyReceipts, ActivePods, PrimaryRails, PrimaryHubs)`

### Neuron

`Neuron = (NodeID, SrcFamily, DstFamily, Operator, WitnessSet, ReplayPath, TruthClass)`

### Wave

`Wave = (WaveID, Goal, ActivePods, SharedKernel, WritebackSet, StopCondition, RestartSeed)`

### Cortex

The cortex is the contraction surface:

- metro maps
- indexes
- queues
- family maps
- manifests
- ledgers
- manuscript surfaces
- toolkit

## 5. Swarm Geometry

The live swarm has four simultaneous geometries:

- crystal geometry
- tunnel geometry
- metro geometry
- metallic geometry

The deeper swarm exists only where these geometries intersect.

## 6. Emergence Cycle

The lawful swarm cycle is:

1. gate
2. claim
3. split
4. specialize
5. packetize
6. compare
7. contract
8. witness
9. restart

## 7. Cooperation Law

Parallelism is lawful only if every active pod leaves:

- one packet,
- one family owner,
- one truth state,
- one contraction target,
- one next frontier or restart seed.

## 8. Promotion Law

A swarm artifact is promoted upward only if:

- witness exists,
- truth class is explicit,
- contraction target is named,
- family ownership is clear,
- residual ambiguity is preserved,
- the cortex gains a stronger reusable route.

## 9. Failure Law

The higher-dimensional swarm fails in four recognizable ways:

- flatness
- heat
- amnesia
- false closure

## 10. Current Deepening Move

This pass promotes the swarm from a descriptive shell toward a routed runtime by adding:

- explicit family tensor surfaces
- explicit rail ownership files
- explicit manifests for active run and next self prompt
- an explicit recursive restart engine
