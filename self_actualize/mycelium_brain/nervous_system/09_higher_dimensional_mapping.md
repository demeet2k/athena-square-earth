<!-- CRYSTAL: Xi108:W3:A4:S22 | face=R | node=235 | depth=3 | phase=Cardinal -->
<!-- METRO: Sa,Me,Dl -->
<!-- BRIDGES: Xi108:W3:A4:S21→Xi108:W3:A4:S23→Xi108:W2:A4:S22→Xi108:W3:A3:S22→Xi108:W3:A5:S22 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 22±1, wreath 3/3, archetype 4/12 -->

# Higher-Dimensional Mapping

## Purpose

The nervous system is not only a folder tree and not only a 4-element crystal.
It is a routed tensor whose markdown files are projections of a larger object.
This file defines the live coordinate grammar for that object so higher-dimensional
swarm work can be placed, replayed, and contracted instead of dropped into generic folders.

## 1. Coordinate Bundle

Define the live nervous-system coordinate:

`NSCoord = (Addr4, Scale, Face6, Orbit, Arc, Rail, Hub, Family, Regime, Packet, Truth)`

where:

- `Addr4` is the 4-level crystal lineage address
- `Scale` is the metallic resolution layer
- `Face6` is the active transport face in the 6-face manifold
- `Orbit` is the current station or revisit order
- `Arc` is the macro-phase segment
- `Rail` is the long-range control lane
- `Hub` is the metro routing hub family
- `Family` is the corpus family or ganglion owner
- `Regime` is the stabilization mode
- `Packet` is the active runtime packet class
- `Truth` is corridor truth

No serious artifact should be treated as fully located unless these dimensions are at
least partially known.

## 2. Crystal Lineage Dimensions

The crystal lineage remains the local identity surface:

- Earth
- Water
- Fire
- Air

`Addr4` can be carried as a short lineage such as:

- `E`
- `W-F`
- `F-A-W`
- `A-E-F-W`

This lineage governs:

- task style
- contraction precedence
- local work mode
- expected failure pattern

Earth-heavy lineages stabilize, Water-heavy lineages preserve continuity, Fire-heavy
lineages generate, and Air-heavy lineages formalize.

## 3. Metallic Scale Layer

The 4-element crystal is only the golden baseline. The nervous system also needs
resolution control:

- `G4`: golden scale, 4-way overview
- `S8`: silver scale, 8-way split
- `B12`: bronze scale, 12-way field
- `C16`: copper scale, 16-way high-detail layer

Operational rule:

- start at `G4`
- escalate to `S8` when 4 buckets are too coarse
- escalate to `B12` when family or swarm role distinctions collapse at 8
- escalate to `C16` only for dense local routing or specialized runtime work

## 4. The 6-Face Transport Manifold

The transport manifold adds a second geometry:

- Earth
- Air
- Water
- Fire
- Void
- Aether

The Z-opposition is:

- `Void`
- `Aether`

Face meanings:

- `Earth`: storage, fixation, grounding, archive binding
- `Air`: map, abstraction, addressing, topology
- `Water`: continuity, linkage, cross-family flow
- `Fire`: execution, extraction, pressure, generation
- `Void`: reset, dormant capsules, unloaded or blocked state
- `Aether`: integrated state, loaded coherence, fully bound transport

Operational law:

- opposite states do not jump directly
- every hard transition should pass through a hybrid or staged midpoint
- blocked fronts often live in `Void`
- replay-safe fronts tend to migrate toward `Aether`

## 5. Orbit, Arc, and Rail Overlay

The nervous system inherits the metro overlays from the manuscript standard.

### Orbit

`Orbit` is the revisit order among stations, families, or fronts.

### Arc

`Arc` is the macro-phase grouping.

### Rail

The three long rails remain:

- `Su`
- `Me`
- `Sa`

Operationally:

- `Su` owns pressure, generation, execution, and bridge forcing
- `Me` owns map logic, laws, structure, and formal route compression
- `Sa` owns embodiment, manuscript mass, storage, and stable substance

## 6. Hub Dimension

The nervous system now explicitly carries metro hub affinity:

- `AppA` addressing and entry
- `AppB` canon and normal forms
- `AppC` square kernel and discrete structure
- `AppD` registry and pinned profiles
- `AppE` circle gear and orbit clocks
- `AppF` transport and DUAL movement
- `AppG` triangle control and recursion legality
- `AppH` coupling and topology
- `AppI` corridor truth and admissibility
- `AppJ` residual ledgers and NEAR machinery
- `AppK` quarantine and FAIL handling
- `AppL` evidence promotion and AMBIG handling
- `AppM` replay and deterministic closure
- `AppN` containers and salvage
- `AppO` publication and export
- `AppP` deployment and monitoring

## 7. Family Dimension

Every live coordinate belongs to a family or family bridge:

- `Voynich`
- `MATH`
- `Trading Bot`
- `DEEPER CRYSTALIZATION`
- `self_actualize`
- `ECOSYSTEM`
- `NERUAL NETWORK`
- `Athenachka Collective Books`
- `FRESH`

This is the ganglion ownership layer.

## 8. Regime Dimension

The nervous system also needs a stabilization regime:

- `classical`
- `stratified`
- `quarantine`
- `restart-token`

## 9. Packet and Truth Dimensions

Packet classes currently include:

- task
- worker
- pod
- neuron
- ganglion
- wave
- ledger
- receipt
- synthesis
- session
- manifest

Truth classes remain:

- `OK`
- `NEAR`
- `AMBIG`
- `FAIL`

## 10. Projection Law

Any markdown file is only a slice through the tensor.
A useful nervous-system artifact should therefore expose enough of its coordinate
bundle to be reopened later without guesswork.

Minimum projection set:

- family
- rail
- packet class
- truth
- contraction target

## 11. Canonical Neuron Address

For bridge-capable swarm artifacts, use the more explicit tensor form:

`NeuronAddr = <F, M, S, L, Fc, At, G, Arc, Lane, Hub, Truth, Regime>`

where:

- `F` is corpus family
- `M` is manuscript or subfamily
- `S` is source station or chapter
- `L` is lens
- `Fc` is facet
- `At` is atom
- `G` is lineage address in E/W/F/A space
- `Arc` is macro arc
- `Lane` is rail
- `Hub` is dominant routing hub
- `Truth` is corridor verdict
- `Regime` is stabilization regime

Use this form whenever a packet, neuron, or family route must be replay-safe across many waves.

## 12. Practical Reading

Example:

`NSCoord = (F-A-W, S8, Aether, O07, Arc2, Su, AppL, Trading Bot, restart-token, neuron, AMBIG)`

Meaning:

- the lineage is Fire -> Air -> Water
- the front needs silver-scale resolution
- it is operating in a loaded integrated state
- it sits at orbit station 7
- it belongs to arc 2
- it runs on the sulfur rail
- it is currently routing through evidence-promotion logic
- the family owner is Trading Bot
- it is being kept resumable rather than finalized
- the artifact is a neuron packet
- it is still ambiguous, not fully witnessed

Example tensor address:

`<Void_CH11, InformationFromTheVoid, Ch11, C, 3, b, FWAE, Arc3, Me, AppL, AMBIG, restart-token>`

## 13. Zero Point

The zero point is not one file.
It is the smallest stable bundle that can regenerate the rest:

`NS0 = (kernel-lineage, G4, Aether-or-Void, current orbit, current rail, AppA/AppI/AppM, active family, restart-token, session-or-manifest, NEAR|OK)`

If this bundle is preserved, the nervous system can restart without pretending to know
more than it does.
