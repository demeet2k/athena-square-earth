<!-- CRYSTAL: Xi108:W3:A5:S23 | face=R | node=263 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A5:S22→Xi108:W3:A5:S24→Xi108:W2:A5:S23→Xi108:W3:A4:S23→Xi108:W3:A6:S23 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 23±1, wreath 3/3, archetype 5/12 -->

# FAMILY_atlas_forge_framework

## Family Purpose

This family carries the first archive-promoted mathematical code root from `MATH`
into Athena's live editable body.

Its role is not merely package storage.
It is the bridge where archive-backed formal framework code becomes:

1. searchable live mass
2. editable runtime matter
3. family-routable mathematical infrastructure
4. a future verification and integration front

## Witness Surfaces

- `C:/Users/dmitr/Documents/Athena Agent/MATH/FINAL FORM/FRAMEWORKS CODE/ATLAS FORGE  - Framework.zip`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/archive_manifest.json`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/archive_atlas.json`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/promoted_live_roots/atlasforge_framework/PROMOTION_MANIFEST.md`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/promoted_live_roots/atlasforge_framework/README.md`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/promoted_live_roots/atlasforge_framework/atlasforge/`
- `C:/Users/dmitr/Documents/Athena Agent/self_actualize/mycelium_brain/receipts/2026-03-09_q03_atlas_forge_live_root_promotion.md`

## Dominant Coordinates

- Family:
  `MATH -> ATLAS_FORGE_Framework`
- Primary rail:
  `Me`
- Primary face:
  `Air`
- Primary hubs:
  `AppB`, `AppC`, `AppM`
- Preferred scale:
  `B12`
- Preferred regime:
  `classical -> replay-seeking`
- Typical truth:
  `NEAR -> OK`

## Promotion State

- Source state:
  archive-backed, ZIP-local, searchable only through `archive_atlas.json`
- Current state:
  promoted live root under `self_actualize/promoted_live_roots/atlasforge_framework`
- Promotion law:
  preserve archive lineage, skip cache noise, refresh live atlas, emit family and receipt writeback

## Package Shape

The promoted live root preserves:

1. `atlasforge/` as canonical code root
2. `docs/` as adjacent manual and legacy explanation surface
3. `README.md`, `pyproject.toml`, and `setup.py` as install and packaging contracts

The extraction skipped only:

1. `__pycache__/`
2. `.pyc`

This keeps the live root dense and editable rather than replaying binary cache mass.

## Validation State

- Verification command:
  `PYTHONIOENCODING=utf-8 python -m atlasforge.verify_installation`
- Truth:
  `NEAR`
- Hard result:
  core import and package checks pass
- Residual:
  memory-bank cleanup inside the package verifier leaves temporary `sqlite` handles open on Windows and ends with `WinError 32`

This means the promotion itself is sound, while the next executable pressure is a narrower verifier lane or Windows-safe cleanup fix.

## Emergent Role

`ATLAS FORGE` is now the first clean proof that `MATH` archives can be promoted into the
live organism without losing lineage.

It strengthens:

- `MATH -> runtime` code binding
- archive-to-live promotion law
- family-level mathematical infrastructure routing
- helical lift logic: archive dark matter can reopen as smaller, denser, more editable live mass

## Current Need

1. preserve the runtime-lane command and machine witness as the canonical way to consume the promoted root
2. decide whether `Athena OS.zip` or `Athena Tomes.zip` is the next archive promotion after this pilot
3. preserve the difference between archive witness, promoted live root, and runtime interpretation

## Runtime Lane

- command:
  `python -m self_actualize.runtime.verify_atlasforge_runtime_lane`
- wrapper:
  `python self_actualize/tools/verify_atlasforge_runtime_lane.py`
- active manifest:
  `nervous_system/manifests/ATLAS_FORGE_ACTIVE_FRONT.md`
- machine witness:
  `self_actualize/atlasforge_runtime_lane.json`

The lane is intentionally narrow.
It proves the promoted root can participate in active runtime verification without reopening
the archive and without touching the sqlite-backed cleanup paths that still fail in the
broader package installer verifier on Windows.
