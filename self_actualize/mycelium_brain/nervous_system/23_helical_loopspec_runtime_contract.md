<!-- CRYSTAL: Xi108:W3:A5:S23 | face=R | node=261 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A5:S22→Xi108:W3:A5:S24→Xi108:W2:A5:S23→Xi108:W3:A4:S23→Xi108:W3:A6:S23 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 23±1, wreath 3/3, archetype 5/12 -->

# Helical LoopSpec Runtime Contract

## Purpose

This surface binds the helical recursion law to a runtime-readable contract.

It links:

1. `ATHENA TEMPLE/04_HELICAL_RECURSION_CADENCE.md`
2. `self_actualize/helix_recursion_schema.json`
3. `ATHENA TEMPLE/CRYSTALS/01_HELICAL_16_LOOP_RECURSION_SCHEMA.md`
4. `receipts/2026-03-09_helical_16_loop_schema.md`

into one control surface that queue, manifest, and future runtime fronts can cite directly.

## Core Law

`(14/16)|n ~= (2/16)|n+1`

`~=` is bridge-equivalence, not scalar equality.

The complement map remains:

`C(k/16) = (16-k)/16`

## Runtime Contract

Every lawful helix pass must declare:

1. a `LoopSpec` with one of the `16` macro mandates
2. a `PhaseSpec` for the current visible address in `2/16 -> 14/16`
3. a `VirtualSwarmSpec` using sparse `TopK` activation over a factorized `16^16` role tensor
4. an `ImprovementLedgerSpec` with `16` mandatory sections
5. a `LiftSpec` obeying the `1/8` compression cap plus monotonicity on function, coverage, and bloat

## Metric Tensor

No pass may lift without auditing all `16` metrics named in `helix_recursion_schema.json`.

## Practical Meaning

The cadence law is no longer only procedural.
It now has a runtime-readable schema that can be cited when a front needs to declare its loop id,
phase address, sparse swarm law, metric state, and lift readiness.
