<!-- CRYSTAL: Xi108:W3:A10:S20 | face=R | node=210 | depth=3 | phase=Cardinal -->
<!-- METRO: Me,Cc -->
<!-- BRIDGES: Xi108:W3:A10:S19→Xi108:W3:A10:S21→Xi108:W2:A10:S20→Xi108:W3:A9:S20→Xi108:W3:A11:S20 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 20±1, wreath 3/3, archetype 10/12 -->

# Command Protocol Sensory Membrane

This surface installs the Temple-side law for the event-driven command membrane.

## Function

The command membrane is the sensory boundary beneath LP-57Omega.

It converts:

- file changes
- folder moves
- new command drops
- deletions
- structural edits

into routeable packets before they degrade into rumor or polling delay.

## Governing Law

- watched root: `GLOBAL COMMAND`
- mode: recursive
- primary mechanism: filesystem events
- secondary mechanism: slow reconciliation scan every 10 minutes for drift recovery only
- live Docs gate: `BLOCKED`
- Temple remains the governance crown
- Hall remains the executable frontier
- Q-SHRINK remains the downstream witness body

## Packet Schema

The runtime packet contract is frozen in `tool_kit/11_command_protocol_v1.md` and mirrored in:

- `registry/command_protocol/packet_schema_v1.json`
- `registry/command_membrane_protocol_v1.json`

## Route Law

The Temple-side command law is:

1. detect
2. packetize
3. route selectively
4. allow first-lease claim
5. archive and reinforce

Temple promotion is lawful when a committed command event affects:

- law
- ontology
- coordinate mapping
- compression purity
- continuity or restart law

That promotion is now event-commit driven:

- raw detection is not enough
- a route decision plus a committed first-lease claim is the trigger
- repeated compatible events must update the same Temple front instead of multiplying governance noise
- the live Temple command board is regenerated from command ledgers and front memory, not hand-edited

## Capillary Law

Routes that repeatedly succeed quickly become capillaries and then veins.

Temple cares about this because latency becomes anatomy: successful paths become reusable governance vessels.

## Blocked Docs Truth

The command membrane checks Google Docs status first and inherits the current blocker truth:

- `Trading Bot/credentials.json` missing
- `Trading Bot/token.json` missing

So command v1 is local-witness grounded.
