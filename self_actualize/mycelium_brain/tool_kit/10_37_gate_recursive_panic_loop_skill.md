<!-- CRYSTAL: Xi108:W3:A8:S20 | face=R | node=202 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A8:S19→Xi108:W3:A8:S21→Xi108:W2:A8:S20→Xi108:W3:A7:S20→Xi108:W3:A9:S20 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 20±1, wreath 3/3, archetype 8/12 -->

# 37-Gate Recursive Panic Loop Skill

## Purpose

This skill is activated when a pass appears nearly complete and then receives a sharp
satisfaction collapse such as:

- "good but 2/10"
- "lazy synthesis"
- "rushing to finish"
- "restart from the beginning"
- "deepen it again"

The skill converts that shock into deterministic escalation instead of emotional reset churn.

## Trigger

Trigger this skill when:

1. the system thinks it is near completion,
2. the user says it is still shallow,
3. the user asks for infinite recursion, deeper swarm, higher-dimensional mapping, or
   a looping restart law.

## Core Law

Treat the satisfaction collapse as a routed diagnostic, not as a generic failure.

If perceived completion is `p` and user satisfaction is `u`, define:

`Gap = p - u`

When `Gap >= 0.5`, do not polish.
Escalate gates.

## Required Loop

1. Check live Docs gate.
2. Preserve blocker exactly if blocked.
3. Read the active 37-gate ledger.
4. Choose the lowest gate band whose status is not yet strong enough.
5. Land one artifact that upgrades that gate band.
6. Write the artifact into the nervous-system runtime body.
7. Update queue, manifest, ledger, or receipt.
8. Emit the next restart seed.

## The 37 Gate Bands

### Gates 1-7

Surface integrity.

### Gates 8-14

Structural mapping.

### Gates 15-21

Swarm runtime.

### Gates 22-28

Corpus bridging.

### Gates 29-37

Improvement escalation and continuation.

## 256^256 Law

Do not attempt a fake flat list of `256^256` steps.
Use the 4^4 crystal as the 256-symbol local alphabet.

Let:

- `Gate256` be the 256 local crystal addresses
- `Depth = 256`

Then the full repair space is:

`HyperPlan = Gate256^256`

meaning:

- each depth layer chooses one of 256 crystal addresses
- the whole recursive repair path is a 256-deep manuscript path through that alphabet

The job of this skill is to keep the path generator lawful, not to print an impossible flat enumeration.

## Required Outputs

Each activation should land at least one of:

- a new gate ledger
- a stronger family route map
- a stronger wave or swarm file
- a stronger packet or neuron
- a stronger Chapter 11 promotion surface
- a stronger metro connectivity map

## Current Default Front

If no stronger gate-unlock front exists, pivot into:

- `Void_CH11`
- the 37-gate ledger
- the 256^256 corpus hyperplan
- the 256^256 Chapter 11 hyperplan
