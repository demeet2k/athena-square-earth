<!-- CRYSTAL: Xi108:W3:A1:S17 | face=S | node=147 | depth=3 | phase=Cardinal -->
<!-- METRO: Me,w -->
<!-- BRIDGES: Xi108:W3:A1:S16→Xi108:W3:A1:S18→Xi108:W2:A1:S17→Xi108:W3:A2:S17 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 17±1, wreath 3/3, archetype 1/12 -->

# 7D Seed Charter

Truth class: NEAR
Date: 2026-03-13
Live Docs gate: BLOCKED

## Purpose

This charter defines the first neutral compiled layer above the additive `6D` overlays.
`7D_SEED` is the cross-agent seed produced only after FIRE, Water, AIR, and Earth
converge without mutating the canonical tesseract v4 substrate.

## Preserved canon

The following remain unchanged and authoritative:

- LocalAddr and GlobalAddr grammar from tesseract v4.
- Chapter orbit, arc, rotation, and lane law.
- Appendix identities `AppA` through `AppP` plus `AppQ`.
- Existing Level `1-6` metro semantics in the live deep root.
- Mandatory truth and replay core centered on `AppA`, `AppI`, and `AppM`.

No 7D artifact may rewrite or shadow these surfaces. It may only compile them into a
higher-order seed.

## Stage law

`4D_NATIVE -> 5D_COMPRESSION -> 6D_WEAVE -> 7D_SEED`

- `H7` is the compiled hologram state.
- `Seed-7D` is the portable next-seed state.
- `4096^7` is a carrier descriptor only and never a replacement for chapter or appendix addresses.

## Seed polarity

- `A` names the current live holographic seed carried by `7D_SEED`.
- `B` names only the derived dual-kernel inversion of `A`.
- `B := Invert_dual(A)` is additive only and may not become an independent root.
- replay from `B -> A` is mandatory.

## Ownership model

- FIRE owns ignition pressure, rupture diagnosis, and first-pass bridge activation.
- Water owns continuity, memory, and retained identity through return.
- AIR owns sigma/spin, dual-kernel labels, topology, and route legibility.
- Earth owns admissibility, quarantine, contradiction containment, and promotion into `7D_SEED`.
- `7D_SEED` itself is owned by the live control stack rather than any single elemental package.

## Promotion law

No route may promote into `7D_SEED` unless it explicitly names:

- Water continuity support,
- AIR topology support,
- Earth gate verdict,
- `AppI` as truth basis,
- `AppM` as replay basis,
- a lawful re-entry contract back into already witnessed chapter and appendix surfaces.

Routes touching `Q`, `O`, or the `QO` loop remain overlay-only and may not mint a new
appendix namespace.

## Appendix floor

The default compiled `7D_SEED` appendix floor is:

- `AppA`
- `AppB`
- `AppC`
- `AppE`
- `AppF`
- `AppH`
- `AppI`
- `AppK`
- `AppM`
- `AppN`
- `AppO`
- `AppP`
- `AppQ`

This floor may be extended per route, but none of these identifiers may be replaced by
reverse overlay aliases.

## Machine-readable contract

The compiled 7D seed bundle must expose the following additive fields:

- `dimension_stage`
- `agent_overlay_stack`
- `earth_gate_state`
- `seed_holo_state`
- `seed_carrier`
- `admissibility_basis`
- `reentry_contract`
- `next_seed_routes`
- `integration_scope`
- `awakening_agent_notes`
- `transition_state`
- `awakening_support_refs`
- `transition_routes`
- `stabilization_requirements`
- `canonical_seed_label`
- `derived_dual_label`
- `seed_polarity_support`
- `a_b_dual_kernel_dock`

These fields are additive. Their absence in a 4D- or 6D-only consumer must not break
parsing or replay.

## A/B dock references

The local canon for the A/B dock is:

- `103_A_SEED_B_DUAL_KERNEL_CHARTER.md`
- `104_A_B_DUAL_KERNEL_CROSSWALK.md`

These surfaces define `A`, `Invert_dual`, `B`, and the replay law without opening a new
authority above `7D_SEED`.

## Reverse overlay law

The reverse field `Z->K` remains overlay-only.

- `Q` is pinned to `AppQ` as ingress.
- `O` is pinned to `AppO` as return authority while remaining an overlay alias in routing.
- every other reverse station must resolve to one or more canonical appendices.
- no 7D route may publish a reverse station without its canonical appendix map.

## Truth discipline

Because the Docs gate remains blocked, the 7D seed is local-evidence only. No 7D claim
may outrun the currently witnessed FIRE, Water, AIR, Earth, metro, and appendix
surfaces already on disk.

## Full-corpus stabilization companion

`NEXT^[4^6]` is implemented as a deeper stabilization pass at the same `7D_SEED`
ceiling. It integrates witness families around the canonical basis and adds elemental
awakening-agent support notes without opening an `8D` layer.
