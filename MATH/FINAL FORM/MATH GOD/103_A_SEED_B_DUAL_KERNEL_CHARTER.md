<!-- CRYSTAL: Xi108:W3:A1:S15 | face=S | node=111 | depth=3 | phase=Cardinal -->
<!-- METRO: Me,w -->
<!-- BRIDGES: Xi108:W3:A1:S14→Xi108:W3:A1:S16→Xi108:W2:A1:S15→Xi108:W3:A2:S15 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 15±1, wreath 3/3, archetype 1/12 -->

# A Seed / B Dual-Kernel Charter

Truth class: NEAR
Date: 2026-03-13
Live Docs gate: BLOCKED

## Purpose

This charter freezes the current live holographic seed as `A` and defines `B` as the
lawful dual-kernel inversion of `A`. This is an additive dock only. It does not create
a new dimensional ceiling, a new appendix namespace, or a competing authority above
`7D_SEED`.

## Canonical law

- `A` is the current live holographic seed already carried by the compiled `7D_SEED`
  bundle and the live `NEXT57` prime-loop state.
- `B` is defined only as `Invert_dual(A)`.
- `B` must remain replayable back into `A`.
- `A` remains canonical. `B` remains derived.

## Dual-kernel operator

`B := Expand_Aether(DualKernel(Collapse_Z(A)))`

Operationally:

1. collapse `A` through the existing zero-point law
2. compute the dual kernel across the current aether / complement relation
3. re-expand as `B`
4. require witnessed replay from `B -> A`

## Preserved invariants

- `LocalAddr` and `GlobalAddr` remain unchanged.
- v4 chapter orbit, arc, rotation, and lane law remain unchanged.
- Appendix identities `AppA` through `AppP` plus `AppQ` remain unchanged.
- No new authority may outrank `7D_SEED`.
- Earth legality remains required on passed A/B routes.
- `AppI` remains the truth basis for passed A/B routes.
- `AppM` remains the replay basis for passed A/B routes.
- `AppQ` and `AppO` remain overlay-only.

## Docking rule

The A/B dock must remain visible in exactly these live surfaces:

- the `7D_SEED` bundle as additive seed-polarity metadata
- the `NEXT57` state and prime protocol as additive polarity support
- the liminal coordinate registry as additive `polarity_state` metadata
- the ledger schema as additive `polarity_state` metadata
- one Guild Hall dock
- one Temple dock

No standalone `B` manuscript family, `B` metro level, or `B` authority root may be
created in this pass.

## Replay rule

No `B` route is complete unless it names a lawful replay path back into `A` through the
existing zero-point and aether relation. The minimum replay statement is:

`B -> Collapse_Z -> Z* -> DualKernel^-1 -> Expand_A`

## Boundary rule

Because the Docs gate remains blocked, the A/B dock is local-evidence only. It may cite
the existing local `7D_SEED`, `NEXT57`, and appendix-governed corpus surfaces, but it
may not claim live Google-doc authority.
