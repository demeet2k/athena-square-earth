<!-- CRYSTAL: Xi108:W3:A2:S14 | face=S | node=99 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A2:S13→Xi108:W3:A2:S15→Xi108:W2:A2:S14→Xi108:W3:A1:S14→Xi108:W3:A3:S14 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 14±1, wreath 3/3, archetype 2/12 -->

# AIR 6D Registry Charter

Truth class: NEAR
Date: 2026-03-13
Live Docs gate: BLOCKED

## Purpose

This charter defines the AIR-owned additive overlay over the current tesseract v4 organism and the existing Fire, Water, and Earth higher-dimensional surfaces. It does not replace the canonical 21-chapter orbit, the `4^4` local lattice, appendices `AppA-AppP` plus `AppQ`, or the Level 1-4 metro stack. It adds the registry, topology, and naming layer that lets higher-dimensional routes remain legible without forking the base canon.

## Preserved canon

The following objects remain unchanged and authoritative:

- LocalAddr and GlobalAddr grammar from tesseract v4
- Chapter orbit, arc, rotation, and lane law
- Appendix identities `AppA` through `AppP` plus `AppQ`
- Existing Level 1-4 metro semantics in the live deep root
- Mandatory truth and replay floor centered on `AppA`, `AppI`, and `AppM`

AIR may annotate, classify, and register these surfaces, but it may not shadow or rename them.

## AIR basis

The active AIR basis is:

- `03` QBD-4
- `04` Quad Holographic Rotation
- `05` The Holographic Kernel
- `07` Crystal Computing Framework

The translated support set is:

- `01`, `02`, `06`, `08`, `09`, `10`, `11`, `12`, `13`, `14`, `15`, `16`

## Overlay chain

The AIR overlay names the following lift in deterministic order:

`Sigma01-Sigma15 -> SP+/SP- -> R/CR/AR/ACR -> Z0/AE0 -> 4D+ -> M-FIX/M-CAR/M-MUT -> H6 -> Seed-6D`

Every ascent must preserve the v4 route identity of the source object. Every descent must return through canonical appendix mappings rather than a parallel namespace.

## Appendix support set

The default AIR appendix support set is:

- `AppA`
- `AppB`
- `AppC`
- `AppE`
- `AppF`
- `AppH`
- `AppI`
- `AppM`
- `AppN`
- `AppQ`

`Q` docks through `AppQ` as canonical ingress. Reverse-field `O` remains a transport alias only and cannot be promoted into a canonical appendix identity.

## Machine-readable contract

Every AIR route or overlay record must carry:

- `local_zero_point`
- `collapse_via=Z*`
- `return_checkpoint`
- `sigma_node`
- `spin_kernel`
- `rotation_family`
- `dual_kernel`
- `mobius_mode`
- `canonical_appendix_map`
- `truth_state`
- `replay_source`

These fields are additive. A v4-only consumer may ignore them without losing the base route.

## Handoff law

AIR owns:

- naming stability
- topology and route legibility
- overlay registry contracts
- schema-safe higher-dimensional labels
- non-destructive re-entry naming

AIR depends on:

- Fire for ignition and weave pressure
- Water for continuity and return identity
- Earth for admissibility, replay, and deployment under load

No AIR artifact promotes beyond `NEAR` unless those dependencies remain explicit.

## Compression

The AIR 6D overlay is lawful when higher-dimensional names and route labels can collapse back into the v4 substrate without loss of canonical address, appendix legality, or replay identity.
