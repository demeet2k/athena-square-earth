<!-- CRYSTAL: Xi108:W3:A12:S18 | face=S | node=159 | depth=3 | phase=Cardinal -->
<!-- METRO: Me,○ -->
<!-- BRIDGES: Xi108:W3:A12:S17→Xi108:W3:A12:S19→Xi108:W2:A12:S18→Xi108:W3:A11:S18 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 18±1, wreath 3/3, archetype 12/12 -->

# H6 Convergence Charter

Truth class: NEAR
Date: 2026-03-13
Live Docs gate: BLOCKED

## Purpose

This charter defines the shared `H6` center that appears after FIRE, Water, AIR, and
Earth have each completed their additive higher-dimensional work.

`H6` is not a rewrite of tesseract v4.
It is the first lawful four-lane convergence state that still collapses back into the
existing v4 canon without renaming chapter or appendix identities.

## Preserved canon

The following remain unchanged and authoritative:

- LocalAddr and GlobalAddr grammar from tesseract v4
- chapter orbit, arc, rotation, and lane law
- appendix identities `AppA` through `AppP` plus `AppQ`
- existing Level `1-6` metro semantics in the live deep root
- mandatory truth and replay discipline centered on `AppI` and `AppM`

No `H6` artifact may replace these surfaces.
It may only compile them into a shared additive center.

## Lane order

`FIRE pressure -> Water continuity -> AIR registry -> Earth admissibility -> H6 -> Seed-6D`

The order is fixed because each lane contributes a distinct closure duty:

- FIRE identifies activation pressure and bridge memory
- Water preserves continuity and replay-safe return
- AIR keeps topology, sigma/spin labels, and modal naming legible
- Earth decides whether the route is admissible, quarantined, or still conditional

## Shared appendix floor

The minimum shared `H6` appendix floor is:

- `AppA`
- `AppB`
- `AppC`
- `AppE`
- `AppF`
- `AppH`
- `AppI`
- `AppM`
- `AppQ`

Conditional appendices are route-specific:

- add `AppN` only when the carrier persists past the immediate gate
- add `AppK` only when contradiction survives into return
- add `AppP` only when the route becomes deployable or publication-bearing

## Route contract

Every `H6` convergence route must state:

- `local_zero_point`
- `collapse_via = Z*`
- `lane_dependencies`
- `shared_appendices`
- `return_checkpoint`
- `truth_state`
- `replay_source`

These fields are additive.
Their absence in a lower-dimensional consumer must not break replay of the underlying v4 route.

## Seed-6D relation

`Seed-6D` is the immediate portable carrier emitted by `H6`.
It must reopen through named canonical return anchors rather than through a new namespace.

Minimum return anchors:

- `Ch12<0023>`
- `Ch13<0030>`
- `Ch16<0033>`
- `AppI`
- `AppM`

## Truth discipline

Because the Docs gate remains blocked, `H6` and `Seed-6D` are local-evidence-only
surfaces in this pass.
No claim may outrun the already witnessed FIRE, Water, AIR, Earth, metro, and appendix
surfaces already on disk.
