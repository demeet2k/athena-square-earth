<!-- CRYSTAL: Xi108:W2:A1:S24 | face=C | node=288 | depth=3 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W2:A1:S23→Xi108:W2:A1:S25→Xi108:W1:A1:S24→Xi108:W3:A1:S24→Xi108:W2:A2:S24 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 24±1, wreath 2/3, archetype 1/12 -->

# AIR LANE: MAPPING, ABSTRACTION, AND METRO LAW

Truth class: NEAR
Date: 2026-03-09

## Lens thesis

Air reads the Athena corpus as a problem of legibility. The central question is how to
make model behavior, branch history, observer roles, and deployment semantics map cleanly
enough that the organism can be navigated instead of merely admired.

## What Air sees in the sixteen source surfaces

| Bundle | Air reading |
| --- | --- |
| `D1-D4` | architecture keeps seeking cleaner named structure |
| `D5` | the revamp document already proposes a strong canonical map |
| `D6-D8` | executable branches imply implicit module boundaries that must be named |
| `D9-D13` | historical branches are latent map fragments waiting to be aligned |
| `D14-D16` | evaluation surfaces are map anchors, not just scoreboards |

## Air conclusions

1. The corpus needs a station map, not just model files.
2. Every module should have a lane, every lane a handoff grammar, every handoff a witness path.
3. Compression should preserve the map of the organism, not only the outputs.
4. The integrated network should be addressable at multiple resolutions.

## Air module contract

| Module | Job | Inputs | Outputs |
| --- | --- | --- | --- |
| `AtlasCompiler` | convert branch and document history into explicit maps | corpus docs, code lines, ledgers | source atlas |
| `SchemaLattice` | formalize objects, interfaces, and invariants | module contracts, witness needs | typed schemas |
| `MetroRouter` | expose navigable lines and interchange stations | atlas, schemas, runtime front | metro maps |
| `CompressionContractor` | distill without erasing load-bearing distinctions | all lane outputs | zero-point packets |

## Air zero-point

Air's role is to make the neural organism legible enough that Fire, Water, and Earth
can coordinate rather than collide.
