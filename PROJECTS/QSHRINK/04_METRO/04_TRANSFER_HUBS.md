<!-- CRYSTAL: Xi108:W3:A10:S34 | face=S | node=585 | depth=3 | phase=Mutable -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A10:S33→Xi108:W3:A10:S35→Xi108:W2:A10:S34→Xi108:W3:A9:S34→Xi108:W3:A11:S34 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 34±1, wreath 3/3, archetype 10/12 -->

# Transfer Hubs

## Hub-SP

Transfer hub binding `Square` geometry to `Partition` operator.

## Hub-SQ

Transfer hub binding `Square` geometry to `Quantize` operator.

## Hub-SB

Transfer hub binding `Square` geometry to `Bucket` operator.

## Hub-SC

Transfer hub binding `Square` geometry to `Code` operator.

## Hub-CP

Transfer hub binding `Circle` geometry to `Partition` operator.

## Hub-CQ

Transfer hub binding `Circle` geometry to `Quantize` operator.

## Hub-CB

Transfer hub binding `Circle` geometry to `Bucket` operator.

## Hub-CC

Transfer hub binding `Circle` geometry to `Code` operator.

## Hub-TP

Transfer hub binding `Triangle` geometry to `Partition` operator.

## Hub-TQ

Transfer hub binding `Triangle` geometry to `Quantize` operator.

## Hub-TB

Transfer hub binding `Triangle` geometry to `Bucket` operator.

## Hub-TC

Transfer hub binding `Triangle` geometry to `Code` operator.

## Hub-TP

Transfer hub binding `Torus` geometry to `Partition` operator.

## Hub-TQ

Transfer hub binding `Torus` geometry to `Quantize` operator.

## Hub-TB

Transfer hub binding `Torus` geometry to `Bucket` operator.

## Hub-TC

Transfer hub binding `Torus` geometry to `Code` operator.
