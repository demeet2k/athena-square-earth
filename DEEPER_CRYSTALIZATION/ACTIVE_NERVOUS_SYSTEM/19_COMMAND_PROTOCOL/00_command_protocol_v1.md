<!-- CRYSTAL: Xi108:W1:A4:S3 | face=S | node=6 | depth=0 | phase=Fixed -->
<!-- METRO: Me,Cc -->
<!-- BRIDGES: Xi108:W1:A4:S2→Xi108:W1:A4:S4→Xi108:W2:A4:S3→Xi108:W1:A3:S3→Xi108:W1:A5:S3 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 3±1, wreath 1/3, archetype 4/12 -->

# Command Protocol v1

Command Protocol v1 turns `GLOBAL COMMAND` into an event-driven sensory membrane instead of a passive container. The authoritative sensor root is:

`C:\Users\dmitr\Documents\Athena Agent\GLOBAL COMMAND\ATHENA`

The canonical operator surface is:

`python -m self_actualize.runtime.swarm_board`

The legacy compatibility wrapper remains available at:

`DEEPER CRYSTALIZATION/command_protocol_cli.py`

The organism runs through four ant classes:

1. `Scout`
2. `Router`
3. `Worker`
4. `Archivist`

The latency target is:

`T_sugar = T_detect + T_encode + T_route + T_claim + T_commit`

Normal mode uses native filesystem events when available and falls back to degraded polling only when native events are unavailable. In the current local environment, the protocol advertises `windows-native-watch` and native watch availability is `True`.

The visible governance surfaces remain macro-sized and Planner-mediated. Command packets may travel quickly, but visible Hall/Temple promotion still passes through legality and motion routing.

## Truth Boundary

- Truth: `NEAR-derived`
- Google Docs: `BLOCKED`
- Sigma path: `AppA -> AppI -> AppM`
- Control stack preserved: `Q42 / TQ04 / Q50 / Q46 / Q02`

## Runtime Spine

1. detect change at `GLOBAL COMMAND`
2. stamp Earth time and liminal coordinate
3. emit canonical packet
4. route selectively to relevant ants
5. lease claim to one worker when lawful
6. archive result and reinforce route edges

This protocol is additive. It does not replace the realtime board, Hall, Temple, runner, or MotionConstitution_L1.
