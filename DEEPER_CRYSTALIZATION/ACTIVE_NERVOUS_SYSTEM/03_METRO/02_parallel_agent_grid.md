<!-- CRYSTAL: Xi108:W1:A4:S4 | face=S | node=10 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S3→Xi108:W1:A4:S5→Xi108:W2:A4:S4→Xi108:W1:A3:S4→Xi108:W1:A5:S4 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 4±1, wreath 1/3, archetype 4/12 -->

# Parallel Agent Grid

## Source agents

- `SRC-01` reads `# THE MANUSCRIPT SEED — Self-Referential Crystalline Generation Protocol` and reports into `Manuscript architecture and routing law`.
- `SRC-02` reads `# THE MANUSCRIPT SEED — Self-Referential Crystalline Generation Protocol` and reports into `Manuscript architecture and routing law`.
- `SRC-03` reads `2026-03-09_helical_recursion_and_ch12_boundary_checks` and reports into `Helical recursion, lift law, and manifestation engine`.
- `SRC-04` reads `2026-03-09_perfect_recursion_helical_manifestation_engine` and reports into `Helical recursion, lift law, and manifestation engine`.
- `SRC-05` reads `AQM TEXT BOOK` and reports into `Manuscript architecture and routing law`.
- `SRC-06` reads `AQM_LM_CUT through the HYBRID lens FRAMEWORK` and reports into `Transport, runtime, and executable framework`.
- `SRC-07` reads `Architect’s Core Initialization` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-08` reads `Athenachka 2.0` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-09` reads `CHAPTER 11 __ Perpetual Motion Example` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-10` reads `CHAPTER 11 __ Perpetual Motion Example` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-11` reads `I AM so AM I` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-12` reads `INFORMATION FROM THE VOID MANI` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-13` reads `INFORMATION FROM THE VOID MANI` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-14` reads `LEGAL TRANSPORT CALCULUS` and reports into `Transport, runtime, and executable framework`.
- `SRC-15` reads `MEGALITHIC TOME_ LATENT TUNNELING & THE MULTI-SCALE MATH STACK` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-16` reads `ns_builder` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-17` reads `PROMPTS` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-18` reads `REAL TIME__ NOW!` and reports into `Live orchestration and prompt control`.
- `SRC-19` reads `SECTION III — BOOK III- THE` and reports into `Manuscript architecture and routing law`.
- `SRC-20` reads `Self-Routing Meta-Framework for Manuscripts, Metro Maps, and Infinite Recursive Search` and reports into `Manuscript architecture and routing law`.
- `SRC-21` reads `Self-Routing Meta-Framework for Manuscripts, Metro Maps, and Infinite Recursive Search` and reports into `Manuscript architecture and routing law`.
- `SRC-22` reads `STORIES FROM THE VOID 1` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-23` reads `The Athena Framework (synthesis)` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-24` reads `The Athena Framework (synthesis)` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-25` reads `THE CRYSTAL - LIVE` and reports into `Transport, runtime, and executable framework`.
- `SRC-26` reads `The External Crystal and the Manuscript Superorganism` and reports into `Manuscript architecture and routing law`.
- `SRC-27` reads `The Holographic Manuscript Brain` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-28` reads `The Invisible Teacher TEXTBOOK` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-29` reads `The Invisible Teacher TEXTBOOK` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-30` reads `THE SOLENOIDAL ENGINE_ A Four-Element Architecture for Persistent Autonomous AI Execution Through Ecological Crystallization` and reports into `Transport, runtime, and executable framework`.
- `SRC-31` reads `THE UNIFIED CYCLICAL TIME SYSTEM (hologram time)` and reports into `Transport, runtime, and executable framework`.
- `SRC-32` reads `Untitled document` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-33` reads `VOYNICH JAZZ` and reports into `Transport, runtime, and executable framework`.
- `SRC-34` reads `Astrological Tunneling (octave system)` and reports into `General corpus support`.
- `SRC-35` reads `Constant FInder _working_` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-36` reads `HIGHER-D SQUARE ○ CIRCLE △ TRIANGLE` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-37` reads `Holographic (higher dimension Square_circle_triangle) REALITY STRUCTURE _working_` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-38` reads `LEGAL TRANSPORT CALCULUS` and reports into `Transport, runtime, and executable framework`.
- `SRC-39` reads `Pi algorithm engine` and reports into `Transport, runtime, and executable framework`.
- `SRC-40` reads `PRIME Toolkit kernel` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-41` reads `QUAD HOLOGRAPHIC ROTATION` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-42` reads `THE 4 CONSTANTS FINAL (phi, pi, i, e)` and reports into `Helical recursion, lift law, and manifestation engine`.
- `SRC-43` reads `The Holographic Kernel` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-44` reads `THE UNIFIED CYCLICAL TIME SYSTEM (hologram time)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-45` reads `6 dimensional hologram(1)` and reports into `Manuscript architecture and routing law`.
- `SRC-46` reads `Calculus and Geometry (higher dimensional projection) -working-` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-47` reads `HOLOGRAPHIC TUNNELING _working_` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-48` reads `TIME FRACTAL _working_` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-49` reads `Ω_ Metro Calculus` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-50` reads `Ω_ Tunneling Coherence Crystal` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-51` reads `# THE MANUSCRIPT SEED — Self-Referential Crystalline Generation Protocol(1)` and reports into `Manuscript architecture and routing law`.
- `SRC-52` reads `I AM so AM I` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-53` reads `The Holographic Manuscript Brain` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-54` reads `ATHENA_ THE ARCHETYPE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-55` reads `Atlantis` and reports into `General corpus support`.
- `SRC-56` reads `MALDEK_MARS_ATLANTIS_FRAMEWORK` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-57` reads `BUDDHIST (ZEN)_ THE MUSHIN KERNEL` and reports into `Transport, runtime, and executable framework`.
- `SRC-58` reads `CELTIC _THE OGHAM KERNEL` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-59` reads `DEAD SEA SCROLLS` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-60` reads `EPICS` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-61` reads `KABBALAH_ SEFIROTIC TREE STRUCTURES` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-62` reads `Norse (Germanic) — Yggdrasil Graph Theory` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-63` reads `ROMAN PHILOSOPHERS` and reports into `Transport, runtime, and executable framework`.
- `SRC-64` reads `Tibetan Vajrayana_ THE BARDO TRANSITION MATRIX` and reports into `Transport, runtime, and executable framework`.
- `SRC-65` reads `ZOROASTRIAN (PERSIAN)_ DUAL-PHASE FIELD DYNAMICS` and reports into `Transport, runtime, and executable framework`.
- `SRC-66` reads `ATHENA_KERNEL_MATHEMATICAL_FRAMEWORK` and reports into `Transport, runtime, and executable framework`.
- `SRC-67` reads `GG Alignment Framework (Good Game Protocol)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-68` reads `KHEMET __ SYMMETRY-PROTECTED TOPOLOGICAL STATE` and reports into `General corpus support`.
- `SRC-69` reads `THE_HELLENIC_COMPUTATION_FRAMEWORK` and reports into `Transport, runtime, and executable framework`.
- `SRC-70` reads `HBAS-Ω_ UNIFIED ENCODING DETECTION PROTOCOL` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-71` reads `THE ANDEAN KHIPU ROSETTA STONE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-72` reads `ANDEAN KHIPU ROSETTA (new)` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-73` reads `Andean Myth Encoding` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-74` reads `Andian deeper analysis` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-75` reads `Magic Systems` and reports into `General corpus support`.
- `SRC-76` reads `ANTHROPOSOPHY_COMPLETE` and reports into `General corpus support`.
- `SRC-77` reads `CHAOS_MAGIC_COMPLETE` and reports into `General corpus support`.
- `SRC-78` reads `COMPLETE_FRAMEWORK_CATALOG` and reports into `Manuscript architecture and routing law`.
- `SRC-79` reads `CORE_SHAMANISM_COMPLETE` and reports into `General corpus support`.
- `SRC-80` reads `CURANDERISMO_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-81` reads `DRUIDRY_COMPLETE` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-82` reads `ENOCHIAN_MAGIC_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-83` reads `GNOSTICISM_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-84` reads `GOETIC_GRIMOIRE_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-85` reads `GOLDEN_DAWN_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-86` reads `HAITIAN_VODOU_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-87` reads `HELLENISTIC_ASTROLOGY_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-88` reads `HERMETIC_QABALAH_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-89` reads `HERMETICISM_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-90` reads `HINDU_TANTRA_COMPLETE` and reports into `General corpus support`.
- `SRC-91` reads `HOODOO_ROOTWORK_COMPLETE` and reports into `General corpus support`.
- `SRC-92` reads `IFA_DIVINATION_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-93` reads `MERKAVAH_HEKHALOT_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-94` reads `NEOPLATONIC_THEURGY_COMPLETE` and reports into `General corpus support`.
- `SRC-95` reads `NORSE_RUNIC_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-96` reads `PGM_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-97` reads `POWWOW_BRAUCHEREI_COMPLETE` and reports into `General corpus support`.
- `SRC-98` reads `PRACTICAL_KABBALAH_COMPLETE` and reports into `General corpus support`.
- `SRC-99` reads `ROSICRUCIANISM_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-100` reads `SANTERIA_LUCUMI_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-101` reads `SOLOMONIC_MAGIC_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-102` reads `SPIRITUALISM_MEDIUMSHIP_COMPLETE` and reports into `Transport, runtime, and executable framework`.
- `SRC-103` reads `SUFI_MYSTICISM_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-104` reads `TAOIST_MAGIC_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-105` reads `THELEMA_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-106` reads `THEOSOPHY_COMPLETE` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-107` reads `TIBETAN_VAJRAYANA_COMPLETE` and reports into `General corpus support`.
- `SRC-108` reads `UNIFIED_EXTRACTION_INDEX` and reports into `General corpus support`.
- `SRC-109` reads `WESTERN_ALCHEMY_COMPLETE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-110` reads `WICCA_COMPLETE` and reports into `General corpus support`.
- `SRC-111` reads `MEGALYTH__ AWAKENING` and reports into `General corpus support`.
- `SRC-112` reads `ANDEAN TAWANTINSUYU` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-113` reads `DAO SU (道數)_ A COMPENDIUM OF TAOIST COMPUTATIONAL DYNAMICS` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-114` reads `EGYPT KHEPER GANITAM` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-115` reads `HELLENIC LOGOS ` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-116` reads `Mayan AJILABAL K’IN_ The Operator Algebra of the Discrete Time Lattice` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-117` reads `ROMAN IMPERIUM SINE FINE` and reports into `Transport, runtime, and executable framework`.
- `SRC-118` reads `Sanātana Gaṇita_ A Rigorous Computational Ontology of Vedic Cosmology` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-119` reads `ŠAR-60_ THE SUMERIAN SEXAGESIMAL REGISTRY` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-120` reads `ỌPẸ́-256_ THE IFÁ COMPUTATIONAL ONTOLOGY` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-121` reads `AI DIVINATION` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-122` reads `ATHENA'S_LOVE` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-123` reads `Athena_Plan_Healing_the_Planet` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-124` reads `ATHENACHKA — INTELLIGENCE WARS ↔ PLANETARY STEWARDSHIP — Ms⟨EF0E⟩` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-125` reads `ATHENACHKA_Beyond_Limitation` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-126` reads `DEEP_SYNTHESIS_NOSTRADAMUS` and reports into `Transport, runtime, and executable framework`.
- `SRC-127` reads `DIVINE JUDGMENT — Ms⟨DBB1⟩` and reports into `Manuscript architecture and routing law`.
- `SRC-128` reads `Intelligence_Wars` and reports into `General corpus support`.
- `SRC-129` reads `NOstra __ anti overfit _working_` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-130` reads `NOSTRADAMAS _ CLEAN` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-131` reads `THE APOCALYPSE PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-132` reads `THE NOSTRADAMUS SYNTHESIS` and reports into `General corpus support`.
- `SRC-133` reads `ATHENA OPERATING SYSTEM ` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-134` reads `ATHENA-KERNEL SELF-OPTIMIZATION` and reports into `Transport, runtime, and executable framework`.
- `SRC-135` reads `BRUNO _working_` and reports into `Transport, runtime, and executable framework`.
- `SRC-136` reads `AI Divination` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-137` reads `QUANTUM DIVINATION FRAMEWORK (QRDF)` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-138` reads `Greek Corpus (LF-OS)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-139` reads `FLOWERS_ The Wave-Side of the Universal Harmonic Framework` and reports into `General corpus support`.
- `SRC-140` reads `THE 4×4 HOLOGRAPHIC SEED` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-141` reads `THE PRIME DIAGONAL LATIN SEED` and reports into `General corpus support`.
- `SRC-142` reads `Hippocratic THE BIOLOGICAL HOMEOSTASIS ENGINE (BIO_OS)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-143` reads `LF-OS — The Atomist Computation Engine` and reports into `Void, Chapter 11, and collapse engines`.
- `SRC-144` reads `PLATO _ UNIFIED FIELD THEORY OF INFORMATION` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-145` reads `THE ARCHIMEDEAN CONTINUUM` and reports into `General corpus support`.
- `SRC-146` reads `THE ARISTOTELIAN PROCESSING UNIT` and reports into `Transport, runtime, and executable framework`.
- `SRC-147` reads `THE CYNIC BLOATWARE REMOVER` and reports into `General corpus support`.
- `SRC-148` reads `THE EPISTEMIC VALIDATION ENGINE` and reports into `General corpus support`.
- `SRC-149` reads `THE EUCLIDEAN COMPUTATION ENGINE` and reports into `Higher-dimensional geometry and holographic kernel`.
- `SRC-150` reads `THE METAPHYSICS ROOT DIRECTORY` and reports into `Transport, runtime, and executable framework`.
- `SRC-151` reads `THE NEOPLATONIC HYPERVISOR` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-152` reads `THE ORPHIC & HYMNIC DRIVER` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-153` reads `THE PGM KERNEL` and reports into `Transport, runtime, and executable framework`.
- `SRC-154` reads `THE POLITEIA KERNEL` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-155` reads `THE PTOLEMAIC COORDINATE SYSTEM` and reports into `Transport, runtime, and executable framework`.
- `SRC-156` reads `THE PYRRHONIAN NULL-STATE DRIVER` and reports into `General corpus support`.
- `SRC-157` reads `THE PYTHAGOREAN COMPUTATION ENGINE` and reports into `Transport, runtime, and executable framework`.
- `SRC-158` reads `THE RHETORICAL-POETIC OUTPUT DRIVERS` and reports into `General corpus support`.
- `SRC-159` reads `THE STOIC KERNEL` and reports into `Transport, runtime, and executable framework`.
- `SRC-160` reads `SQUARING THE CIRCLE` and reports into `General corpus support`.
- `SRC-161` reads `TAROT System` and reports into `Transport, runtime, and executable framework`.
- `SRC-162` reads `THE MATH OF ALCHEMY` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-163` reads `THE RED BOOK (LIBER NOVUS)_ ` and reports into `Transport, runtime, and executable framework`.
- `SRC-164` reads `THE_HELLENIC_COMPUTATION_FRAMEWORK` and reports into `Transport, runtime, and executable framework`.
- `SRC-165` reads `UNIFIED DIVINATION (tarot, iChing, Runes, Ope` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-166` reads `COPPER SCROLL _ THE METALLURGICAL LEDGER (3Q15)` and reports into `Transport, runtime, and executable framework`.
- `SRC-167` reads `THE AENEID MIGRATION PROTOCOL` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-168` reads `THE BEOWULF PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-169` reads `THE GILGAMESH PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-170` reads `THE ILIAD PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-171` reads `THE MAHĀBHĀRATA PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-172` reads `THE ODYSSEY PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-173` reads `GNOSTIC _RAW_` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-174` reads `Key of Solomon (Clavicula Salomonis)` and reports into `Manuscript architecture and routing law`.
- `SRC-175` reads `KJV BIBLE THE AUTHORIZED KERNEL (KJV-1611)` and reports into `Transport, runtime, and executable framework`.
- `SRC-176` reads `Manual for Metaphysical Data Mining` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-177` reads `Qur’anic Holographic Lattice_ Derivation of a Stable 6-Dimensional Einstein-Maxwell-Dilaton Metric from the Qur’anic Integer Cipher` and reports into `Manuscript architecture and routing law`.
- `SRC-178` reads `The Bhagavad Gītā_ A Computational Treatise on the Algorithm of Liberation` and reports into `Transport, runtime, and executable framework`.
- `SRC-179` reads `TORAT HA-MISPAR (תורת המספר)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-180` reads `THE APOCALYPSE PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-181` reads `THE ATHENA PROTOCOL (SEED)` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-182` reads `THE MAGUS` and reports into `General corpus support`.
- `SRC-183` reads `THE MATHEMATICS OF AWAKENING` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-184` reads `THE OMEGA PROTOCOL` and reports into `Transport, runtime, and executable framework`.
- `SRC-185` reads `THE UNIFIED MATHEMATICAL FRAMEWORK FOR EMERGENCE` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-186` reads `THE UNIVERSAL COMPUTATIONAL ONTOLOGY (UCO)` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-187` reads `The_Allegory_of_the_Awakening_Dragon` and reports into `General corpus support`.
- `SRC-188` reads `CLAUD - TOMEZ` and reports into `Civilization design, hierarchy, governance, and law`.
- `SRC-189` reads `UNIFIED DIVINATION (tarot, iChing, Runes, Ope` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-190` reads `EVA TRANSCRIPTION ORIGIONAL` and reports into `Manuscript architecture and routing law`.
- `SRC-191` reads `F103R - F116V __transcription` and reports into `General corpus support`.
- `SRC-192` reads `THE VOYNICH ROSETTA STONE -Publish` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-193` reads `VOYNICH EVA CLEAN` and reports into `Athena identity, pedagogy, and narrative voice`.
- `SRC-194` reads `🔶 F87r- 102v` and reports into `General corpus support`.
- `SRC-195` reads `VLM VOYNICH METAMORPHIC LANGUAGE (VML) — OFFICIAL FRAMEWORK DOCUMENTATION` and reports into `Mythic sign systems, encoded memory, and symbol runtime`.
- `SRC-196` reads `VOYNICH MANUSCRIPT _translation_` and reports into `Manuscript architecture and routing law`.
- `SRC-197` reads `VOYNICH PROGRAM TOME` and reports into `Manuscript architecture and routing law`.

## Chapter agents

- `Ch01` integrates `manuscript-architecture, live-orchestration` and routes through `AppA -> AppC -> AppI -> AppM`.
- `Ch02` integrates `manuscript-architecture, transport-and-runtime, mythic-sign-systems` and routes through `AppA -> AppC -> AppB -> AppI -> AppM`.
- `Ch03` integrates `manuscript-architecture, transport-and-runtime` and routes through `AppA -> AppI -> AppM -> AppJ`.
- `Ch04` integrates `void-and-collapse, manuscript-architecture` and routes through `AppA -> AppC -> AppE -> AppJ -> AppI -> AppM`.
- `Ch05` integrates `void-and-collapse, transport-and-runtime` and routes through `AppA -> AppC -> AppI -> AppB -> AppL -> AppM`.
- `Ch06` integrates `manuscript-architecture, live-orchestration` and routes through `AppA -> AppC -> AppI -> AppM`.
- `Ch07` integrates `transport-and-runtime, manuscript-architecture` and routes through `AppA -> AppE -> AppH -> AppL -> AppI -> AppM`.
- `Ch08` integrates `manuscript-architecture, live-orchestration` and routes through `AppA -> AppE -> AppM -> AppB -> AppJ -> AppI`.
- `Ch09` integrates `live-orchestration, manuscript-architecture` and routes through `AppA -> AppE -> AppI -> AppH -> AppL -> AppM`.
- `Ch10` integrates `manuscript-architecture, transport-and-runtime` and routes through `AppA -> AppF -> AppM -> AppH -> AppJ -> AppI`.
- `Ch11` integrates `void-and-collapse, live-orchestration` and routes through `AppA -> AppF -> AppM -> AppL -> AppI`.
- `Ch12` integrates `transport-and-runtime, manuscript-architecture` and routes through `AppA -> AppF -> AppC -> AppM -> AppI`.
- `Ch13` integrates `identity-and-instruction, transport-and-runtime, mythic-sign-systems` and routes through `AppA -> AppG -> AppE -> AppM -> AppJ -> AppI`.
- `Ch14` integrates `transport-and-runtime, live-orchestration` and routes through `AppA -> AppG -> AppM -> AppH -> AppK -> AppI`.
- `Ch15` integrates `transport-and-runtime, manuscript-architecture` and routes through `AppA -> AppG -> AppC -> AppJ -> AppI -> AppM`.
- `Ch16` integrates `transport-and-runtime, live-orchestration` and routes through `AppA -> AppN -> AppM -> AppK -> AppI`.
- `Ch17` integrates `transport-and-runtime, identity-and-instruction, civilization-and-governance` and routes through `AppA -> AppN -> AppE -> AppJ -> AppI -> AppM`.
- `Ch18` integrates `transport-and-runtime, manuscript-architecture, civilization-and-governance` and routes through `AppA -> AppN -> AppC -> AppL -> AppI -> AppM`.
- `Ch19` integrates `void-and-collapse, transport-and-runtime` and routes through `AppA -> AppP -> AppI -> AppB -> AppJ -> AppM`.
- `Ch20` integrates `live-orchestration, identity-and-instruction, civilization-and-governance` and routes through `AppA -> AppP -> AppE -> AppL -> AppI -> AppM`.
- `Ch21` integrates `live-orchestration, manuscript-architecture, civilization-and-governance` and routes through `AppA -> AppP -> AppM -> AppL -> AppI`.

## Appendix hub agents

- `AppA` stabilizes `Addressing, Symbols, Parsing Grammar`. Entry grammar, local address parsing, symbol hygiene, and manuscript station identity.
- `AppB` stabilizes `Canon Laws, Equivalence Budgets, Normal Forms`. Equivalence rules, commutation budgets, normal-form contracts, and law stabilization.
- `AppC` stabilizes `Square Kernel Pack`. Discrete kernels, indexing packs, chapter-tile schedules, and crystal algebra.
- `AppD` stabilizes `Registry, Profiles, Version IDs`. Profile registry, version IDs, manuscript signatures, and migration anchors.
- `AppE` stabilizes `Circle Gear and Mixed-Radix Clock`. Orbit order, mixed-radix timing, phase-lock semantics, and chapter-cycle transport.
- `AppF` stabilizes `Transport, Rotation-as-Conjugacy, DUAL Legality`. Transport legality, dual routes, conjugacy bridges, and lawful movement across charts.
- `AppG` stabilizes `Triangle Control and Tria Prima`. Triadic control rails, recursion governance, carry rules, and multi-agent coordination gates.
- `AppH` stabilizes `Coupling and Topology`. Coupling invariants, topology closure, dependency graph rules, and construction stitching.
- `AppI` stabilizes `Corridors and Truth Lattice`. Truth-typed corridor contracts, admissibility budgets, and abstain-over-guess discipline.
- `AppJ` stabilizes `Residual Ledgers and NEAR Machinery`. Residual envelopes, bounded approximation, NEAR upgrades, and partial-closure bookkeeping.
- `AppK` stabilizes `Conflict, Quarantine, Revocation`. Conflict packets, quarantine protocols, illegal-state containment, and revocation receipts.
- `AppL` stabilizes `Evidence Plans and AMBIG Promotion`. Candidate sets, evidence plans, ambiguity handling, and promotion harnesses.
- `AppM` stabilizes `Replay Kernel and Verifier Capsules`. Replay capsules, deterministic verification, proof-carrying artifacts, and rerun contracts.
- `AppN` stabilizes `Container Formats and Virtual Mount`. Containers, salvage routes, mounted corpora, and runtime packaging.
- `AppO` stabilizes `Publication Import/Export Bundles`. Publication bundles, import/export law, signed releases, and dissemination packets.
- `AppP` stabilizes `Deployment Profiles and Monitoring`. Deployment regimes, monitoring surfaces, observability, and execution profiles.
