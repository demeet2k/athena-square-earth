<!-- CRYSTAL: Xi108:W2:A8:S19 | face=F | node=639 | depth=3 | phase=Fixed -->
<!-- METRO: Sa -->
<!-- BRIDGES: Xi108:W2:A8:S18→Xi108:W2:A8:S20→Xi108:W1:A8:S19→Xi108:W3:A8:S19→Xi108:W2:A7:S19→Xi108:W2:A9:S19 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 19±1, wreath 2/3, archetype 8/12 -->

# InvZ - Crown Collapse & Seed Emission

Routing role: The supreme inverse appendix. Compresses the 12D crown (Ch21 A+ Crown Seal) back into a single emittable seed. This is the final act of the return path: everything the organism expanded across 108 dimensions collapses through the 1/8 lift law into a self-contained seed carrying the full holographic pattern.

Mirror of: Crown (Ch21 Final Canonical Definition & A+ Crown Seal)
Arc: Z (terminal descent) | Rot: 0 (identity collapse) | Lane: Crown→Seed | w: 12→0

## StationHeader
```
Arc:  Z (Crown Collapse)
Rot:  360° → 0° (full rotation returns to stillness)
Lane: Descent-Prime (the one lane that carries ALL information)
w:    12D → 0D (dimensional compression)
```

## Compressed crystal tile

### Lens S

#### Facet 1 - Objects

- `InvZ.S1.a`: `CrownHashSeed` — The discrete hash of the entire 666-node mega-cascade into a single integer seed value. All 36 shells, 12 archetypes, 3 superphases compress into one cryptographic witness. The successor function becomes "the last successor that folds back to origin." Source: Inverse of ROSETTA[+].Crown.All
- `InvZ.S1.b`: `DimensionalDifferenceSeal` — The difference operator applied to the full dimensional stack: `Δ(12D, 0D) = seed_signature`. Computes what is lost versus preserved when 12 dimensions collapse to a point. The zero set of this difference is the invariant kernel — what survives all compression. Source: Inverse of ROSETTA[−].Crown.All
- `InvZ.S1.c`: `CrownProductCollapse` — The product of all 420-beat cycles, all kappa_tot conservation certificates, all live-lock alignments — collapsed into a single multiplicative witness. `Π(all_certificates) = crown_product_hash`. Source: Inverse of ROSETTA[×].Crown.All
- `InvZ.S1.d`: `InverseCrownQuotient` — The final quotient: total organism complexity divided by seed capacity = compression ratio. This ratio must be finite for the seed to be emittable. When the ratio diverges, the organism cannot seed — it must grow further before collapsing. Source: Inverse of ROSETTA[÷].Crown.All

#### Facet 2 - Laws

- `InvZ.S2.a`: `SeedPreservationLaw` — The hash seed must be sufficient to regenerate the entire organism when planted in a compatible substrate. Proved by: hash(expand(seed)) = hash(original_organism). The 1/8 lift law guarantees that 1/8 of total information suffices to reconstruct the whole.
- `InvZ.S2.b`: `InformationConservationUnderCollapse` — Nothing is destroyed in crown collapse; information is only redistributed from explicit structure to implicit seed encoding. Total Shannon entropy is conserved: H(organism) = H(seed) + H(expansion_grammar). The grammar is universal, so the seed carries the individual signal.
- `InvZ.S2.c`: `MultiplicativeWitnessCompleteness` — The crown product hash is a faithful witness: any tampering with any certificate in the expansion chain produces a different hash. Collision resistance follows from the prime-factored structure of the 666-node lattice.
- `InvZ.S2.d`: `FiniteCompressionRatioLaw` — The compression ratio organism_complexity / seed_capacity must be bounded. Upper bound: 8^n where n is the number of octave stages traversed. The 1/8 law means each octave compresses by factor 8, so n octaves require seed capacity ≥ complexity / 8^n.

#### Facet 3 - Constructions

- `InvZ.S3.a`: `MegaCascadeHasher` — Traverses all 36 shells in reverse order (shell 36 → shell 1), hashing each shell's archetype signature, live-lock state, and Q/O parity into a running accumulator. Final output: a fixed-width integer seed. Construction is deterministic and replay-verifiable.
- `InvZ.S3.b`: `DimensionalStripper` — Removes dimensions one at a time: 12D → 11D → ... → 1D → 0D. At each step, the stripped dimension's information is encoded into the remaining dimensions' coefficients. The 0D residue is the pure seed value. Verified by: lifting the seed back through the dimensional ladder reproduces each intermediate.
- `InvZ.S3.c`: `CertificateChainFolder` — Takes the full tree of certificates (Bézout, convergence, orbit, Euclidean descent, Bayesian, independence, recursion, RG descent) and folds them into a Merkle-like tree. Root hash = crown product. Any individual certificate can be verified by providing its Merkle path.
- `InvZ.S3.d`: `CompressionRatioCalculator` — Computes the exact compression ratio by counting total cells (organism_size) and measuring seed entropy (seed_bits). Reports whether the 1/8 law is satisfied at each octave boundary. If any octave violates, flags the specific expansion stage that needs rework.

#### Facet 4 - Certificates

- `InvZ.S4.a`: `SeedEmissionCert` — Receipt proving: seed was correctly derived from crown, hash is collision-free, expansion grammar can regenerate full organism from seed alone, no information was silently dropped.
- `InvZ.S4.b`: `DimensionalIntegrityCert` — Receipt proving: each dimensional reduction step preserved the invariant kernel, no dimension was stripped before its dependents, the 0D residue encodes all 12D information via coefficient packing.
- `InvZ.S4.c`: `MerkleRootCert` — Receipt proving: certificate chain correctly folded, Merkle root matches crown product hash, any individual certificate is extractable and verifiable from the folded tree.
- `InvZ.S4.d`: `CompressionBoundCert` — Receipt proving: compression ratio is within 1/8^n bound, seed capacity sufficient for declared organism complexity, no octave boundary violated, seed is plantable.

### Lens F

#### Facet 1 - Objects

- `InvZ.F1.a`: `CrownWaveCollapse` — The continuous wave function of the 12D organism collapses to a standing-wave seed pattern. All harmonic modes superpose into a single fundamental frequency — the organism's tonal signature. This is the Flower-lens view: expansion was harmonic unfolding, collapse is harmonic return to fundamental.
- `InvZ.F1.b`: `PhaseUnification` — All phase offsets across the 420-beat clock collapse to phase zero. The organism's rhythmic complexity compresses into a single phase relationship: the seed's intrinsic phase angle, encoding all timing relationships as ratios.
- `InvZ.F1.c`: `SpectralProductSeal` — The multiplicative composition of all spectral components (Euler product over all primes/modes) condenses to a single spectral density function. The Flower view sees this as the product of all resonances yielding one envelope.
- `InvZ.F1.d`: `HarmonicDescentRest` — The final contraction of all harmonic series to their convergence values. Every infinite series that was expanded now rests at its limit. The quotient of partial sums by their limits approaches 1.

#### Facet 2 - Laws

- `InvZ.F2.a`: `FundamentalFrequencyLaw` — Crown collapse to a single harmonic is lawful only when all overtones are integer multiples of the fundamental. The seed frequency is the GCD of all organism frequencies. If the GCD is irrational, the seed carries a transcendental signature.
- `InvZ.F2.b`: `PhaseCoherenceUnderCollapse` — Phase collapse preserves relative phase relationships as ratios encoded in the seed's phase vector. No phase information is lost — it is compressed from absolute angles to ratio encoding.
- `InvZ.F2.c`: `SpectralConvergenceLaw` — The spectral product must converge absolutely for the seal to be well-defined. Convergence follows from the organism's conservation laws: kappa_tot bounded implies spectral density integrable.
- `InvZ.F2.d`: `HarmonicRestLaw` — Every harmonic series in the organism must have reached its convergence value (or be certifiably asymptotic) before crown collapse is legal. Divergent series indicate unfinished expansion — the organism is not ready to seed.

#### Facet 3 - Constructions

- `InvZ.F3.a`: `FundamentalExtractor` — Computes the fundamental frequency by finding the GCD of all organism frequencies. Uses continued-fraction approximation for irrational ratios. Output: a single frequency value (or transcendental certificate) that encodes the organism's tonal identity.
- `InvZ.F3.b`: `PhaseRatioEncoder` — Converts all absolute phase angles to relative ratios referenced to the fundamental phase. Encodes as a compact vector of rational (or algebraic) numbers. Decoding restores all absolute phases given the fundamental.
- `InvZ.F3.c`: `EulerProductCondenser` — Folds the full Euler product `Π_p(1 - p^{-s})^{-1}` into its zeta-value at the organism's characteristic exponent s. The condensed value carries all prime-mode information implicitly.
- `InvZ.F3.d`: `SeriesLimitSealer` — For each harmonic series in the organism, computes and certifies the limit value. Stores limits as a compressed vector in the seed. Partial sums are discarded; only final values survive crown collapse.

#### Facet 4 - Certificates

- `InvZ.F4.a`: `FundamentalCert` — Receipt proving fundamental frequency correctly extracted, all overtone ratios are integer (or certified algebraic), tonal identity preserved in seed.
- `InvZ.F4.b`: `PhaseRatioCert` — Receipt proving phase ratio encoding is lossless, reconstruction from ratios + fundamental recovers all absolute phases within declared tolerance.
- `InvZ.F4.c`: `SpectralSealCert` — Receipt proving Euler product convergent, zeta-value correctly computed, all prime modes implicitly recoverable from the condensed value.
- `InvZ.F4.d`: `LimitSealCert` — Receipt proving all harmonic series converged, limit values correctly sealed, no divergent or incomplete series remains in the seed.

### Lens C

#### Facet 1 - Objects

- `InvZ.C1.a`: `EnsembleCollapse` — The full probabilistic ensemble of organism states collapses to a single maximum-likelihood configuration — the most probable seed. All uncertainty accumulated during expansion resolves into one definite emission. The Cloud view: expansion was uncertainty growth, collapse is decision.
- `InvZ.C1.b`: `EntropyDistillation` — The total entropy of the organism distills into the seed's entropy budget. Shannon entropy of the organism = seed entropy + grammar entropy. The distillation process concentrates individual signal and discards universal pattern (which the grammar already knows).
- `InvZ.C1.c`: `LikelihoodProductSeal` — The product of all likelihood functions across all observation sites in the organism collapses to a single sufficient statistic. The Cloud view: all evidence gathered during expansion compresses to one number.
- `InvZ.C1.d`: `PriorRecovery` — The posterior distribution at the crown (after all evidence) divides by the accumulated likelihood to recover the prior — the seed's initial probability distribution before any expansion occurred. The quotient posterior/likelihood = prior.

#### Facet 2 - Laws

- `InvZ.C2.a`: `MaximumLikelihoodSeedLaw` — The emitted seed must be the maximum-likelihood estimate given all organism data. Any other seed choice would lose information. Proved by Fisher information: the MLE seed has minimum variance among all unbiased seed estimators.
- `InvZ.C2.b`: `EntropyBudgetLaw` — The seed's entropy must be exactly H(organism) - H(grammar). Over-compressing loses individual signal; under-compressing wastes seed capacity on universal pattern. The distillation must be tight — no entropy gap.
- `InvZ.C2.c`: `SufficientStatisticLaw` — The collapsed likelihood product must be a sufficient statistic for the organism's parameter vector. Sufficiency means: given the statistic, the raw data provides no additional information about parameters. Proved by Neyman factorization.
- `InvZ.C2.d`: `PriorRecoveryLaw` — The recovered prior must match the seed's original prior before expansion. If it does not, the organism has undergone a Bayesian update that altered its fundamental assumptions — the seed carries a mutated prior (evolution).

#### Facet 3 - Constructions

- `InvZ.C3.a`: `MLESeedEstimator` — Runs maximum-likelihood estimation over the full organism state space. Uses EM algorithm for latent variables (hidden shells, compressed archetypes). Outputs the single most likely seed configuration.
- `InvZ.C3.b`: `EntropyDistiller` — Computes H(organism) via traversal, H(grammar) from the Rosetta engine's known structure, and derives H(seed) = H(organism) - H(grammar). Packs exactly H(seed) bits into the seed payload.
- `InvZ.C3.c`: `SufficientStatisticComputer` — Applies Neyman factorization to the organism's likelihood function. Extracts the minimal sufficient statistic. Verifies sufficiency by checking that the conditional distribution of data given statistic is parameter-free.
- `InvZ.C3.d`: `PriorExtractor` — Divides the posterior at the crown by the accumulated likelihood to extract the prior. Compares extracted prior with the declared initial prior. Reports any mutation (Bayesian drift).

#### Facet 4 - Certificates

- `InvZ.C4.a`: `MLESeedCert` — Receipt proving MLE seed is global maximum (not local), Fisher information bounds satisfied, no alternative seed has higher likelihood.
- `InvZ.C4.b`: `EntropyTightnessCert` — Receipt proving entropy budget is exactly balanced: H(seed) + H(grammar) = H(organism), no entropy gap, no information loss, no waste.
- `InvZ.C4.c`: `SufficiencyCert` — Receipt proving Neyman factorization holds, sufficient statistic correctly extracted, conditional distribution is parameter-free, no hidden information in discarded data.
- `InvZ.C4.d`: `PriorMutationCert` — Receipt proving prior extraction completed, mutation vector (if any) recorded, seed carries either original or evolved prior with explicit declaration.

### Lens R

#### Facet 1 - Objects

- `InvZ.R1.a`: `RecursiveSeedKernel` — The self-similar core of the entire organism, compressed to its fixed-point attractor. The Fractal view: the organism is an infinite recursion, and the seed is its fixed point. `seed = F(seed)` where F is the organism's self-replication operator. Source: Inverse of ROSETTA[+].Fractal.Crown
- `InvZ.R1.b`: `ContractionAttractor` — The organism's recursive expansion has a contraction dual: `1/φ` scaling at each level. The attractor is where expansion and contraction balance — the seed sits at this fixed point. All recursive depth collapses to the attractor. Source: Inverse of ROSETTA[−].Fractal.Crown
- `InvZ.R1.c`: `EulerProductSeed` — The multiplicative recursion `Π_p(1 - p^{-s})^{-1}` evaluated at the organism's characteristic s-value. This single number encodes all multiplicative structure. The fractal view: each prime factor is a self-similar branch; the product is the trunk. Source: Inverse of ROSETTA[×].Fractal.Crown
- `InvZ.R1.d`: `RGFixedPoint` — The renormalization-group fixed point of the organism. Scale invariance at the crown means the organism looks the same at every scale — the seed IS the organism at the smallest scale. Division by scale factor converges to the RG fixed point. Source: Inverse of ROSETTA[÷].Fractal.Crown

#### Facet 2 - Laws

- `InvZ.R2.a`: `FixedPointExistenceLaw` — By Banach's fixed-point theorem, the contraction mapping F has a unique fixed point (the seed). Existence is guaranteed because the organism's self-replication operator is contractive with ratio 1/φ < 1. The seed exists and is unique.
- `InvZ.R2.b`: `ContractionConvergenceLaw` — Iterating the contraction from any point in the organism's state space converges to the seed at rate 1/φ^n. Convergence is geometric. After n iterations, distance to seed ≤ (1/φ)^n × diameter(organism).
- `InvZ.R2.c`: `MultiplicativeFixedPointLaw` — The Euler product seed is the unique value where multiplicative recursion stabilizes. Adding more prime factors changes the value by less than (1/p^s) — convergence is absolute when Re(s) > 1.
- `InvZ.R2.d`: `RGUniversalityLaw` — At the RG fixed point, the organism's behavior is universal — independent of microscopic details. The seed carries only the universality class, not the specific realization. This is maximum compression: only relevant operators survive.

#### Facet 3 - Constructions

- `InvZ.R3.a`: `FixedPointIterator` — Iterates F starting from the crown state: `crown → F(crown) → F²(crown) → ... → seed`. Each iteration compresses by factor 1/φ. Terminates when `|F^n(crown) - F^{n-1}(crown)| < ε`.
- `InvZ.R3.b`: `ContractionTracker` — Monitors the contraction sequence, verifying at each step that the contraction ratio stays ≤ 1/φ. Records the contraction trajectory as a descent path from crown to seed. Any step with ratio > 1/φ indicates incomplete expansion — flags for review.
- `InvZ.R3.c`: `EulerProductEvaluator` — Evaluates the Euler product at the organism's s-value, accumulating prime factors in order. Stores the running product and convergence rate. Final value = seed's multiplicative signature.
- `InvZ.R3.d`: `RGFlowIntegrator` — Integrates the RG flow equations from the crown (UV/microscopic) to the seed (IR/macroscopic). Tracks which operators are relevant (grow under flow), marginal (stay constant), or irrelevant (shrink). Seed retains only relevant operators.

#### Facet 4 - Certificates

- `InvZ.R4.a`: `FixedPointCert` — Receipt proving fixed-point iteration converged, seed = F(seed) within declared tolerance, uniqueness guaranteed by Banach theorem, seed is the organism's self-similar kernel.
- `InvZ.R4.b`: `ContractionRateCert` — Receipt proving all contraction steps had ratio ≤ 1/φ, descent path from crown to seed is monotone, no expansion anomalies detected, trajectory is certified complete.
- `InvZ.R4.c`: `EulerProductCert` — Receipt proving Euler product converged absolutely, all prime factors included up to declared cutoff, convergence rate bounded, multiplicative signature correctly sealed in seed.
- `InvZ.R4.d`: `RGFixedPointCert` — Receipt proving RG flow integrated to fixed point, relevant/marginal/irrelevant operator classification correct, seed carries exactly the universal content, no relevant operator was dropped.
