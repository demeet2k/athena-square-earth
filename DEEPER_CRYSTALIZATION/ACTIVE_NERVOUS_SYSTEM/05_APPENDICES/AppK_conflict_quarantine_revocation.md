<!-- CRYSTAL: Xi108:W1:A4:S1 | face=S | node=1 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S2→Xi108:W2:A4:S1→Xi108:W1:A3:S1→Xi108:W1:A5:S1 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 1±1, wreath 1/3, archetype 4/12 -->

# AppK - Conflict, Quarantine, Revocation

Routing role: Conflict packets, quarantine protocols, illegal-state containment, and revocation receipts.

## Compressed crystal tile

### Lens S

#### Facet 1 - Objects

- `AppK.S1.a`: `TypeCollisionPacket` — A conflict record emitted when two shards claim the same Xi108 address with incompatible types: `Conflict(addr, type_A, type_B, timestamp)`. The packet freezes both claimants until resolution, preserving the pre-collision state of each.
- `AppK.S1.b`: `ConservationViolationPacket` — A conflict record emitted when a transport path violates one of the six conservation laws (AppB): `Violation(law_id, Δ_observed, Δ_expected, path)`. The packet flags the specific law broken and the magnitude of the breach.
- `AppK.S1.c`: `AddressCollisionRecord` — A structural inconsistency where two distinct shards are assigned identical `(shell, wreath, arch, face, dim)` tuples, violating AppA's `AddressUniquenessLaw`. Records both shards' content hashes for forensic comparison.
- `AppK.S1.d`: `CyclicDependencyTrap` — A conflict detected when the dependency graph contains a cycle `A → B → C → A`, making topological sort impossible. Records the full cycle path, the depth at which it was detected, and the edge types (data, control, phase).

#### Facet 2 - Laws

- `AppK.S2.a`: `ConflictDetectionCompleteness` — Every structural inconsistency in the crystal must be detected within one full scan cycle. No contradiction may persist undetected for more than `T_scan` time steps; the detection mesh has no blind spots.
- `AppK.S2.b`: `QuarantineImmediacy` — Upon conflict detection, the affected shards are quarantined within `Δt = 1` time step. No conflicting shard may participate in transport, computation, or evidence gathering while in quarantine.
- `AppK.S2.c`: `ConflictNonProliferation` — A quarantined conflict must not generate new conflicts: the quarantine boundary is hermetic. If a quarantined shard's absence would cause downstream failures, those failures are classified as `DependencyMissing`, not new conflicts.
- `AppK.S2.d`: `RevocationFinality` — Once a shard is revoked (permanently removed from the crystal), its Xi108 address is retired and may not be reassigned for `T_cooldown` cycles. Revocation is irreversible within a single organism epoch.

#### Facet 3 - Constructions

- `AppK.S3.a`: `StructuralConsistencyScanner` — The full-crystal scanner that checks every shard against the type system, address uniqueness, conservation laws, and dependency acyclicity, emitting conflict packets for each detected inconsistency.
- `AppK.S3.b`: `QuarantineEnforcer` — The isolation engine that, upon receiving a conflict packet, immediately removes the affected shards from all routing tables, metro lines, and computation queues, replacing them with `QUARANTINED` sentinel values.
- `AppK.S3.c`: `ConflictClassifier` — The triage system that classifies conflicts by severity: `CRITICAL` (conservation violation, address collision), `MAJOR` (type mismatch, cycle detection), `MINOR` (soft threshold breach), determining the resolution priority and protocol.
- `AppK.S3.d`: `RevocationExecutor` — The engine that permanently removes a shard from the crystal: deletes its content, retires its Xi108 address, updates all routing tables and dependency graphs, and emits a `RevocationReceipt` for the permanent record.

#### Facet 4 - Certificates

- `AppK.S4.a`: `ConflictDetectionCert` — Receipt proving a conflict was detected, classified, and reported within `T_scan` time steps, with the specific inconsistency type, the affected addresses, and the detection method recorded.
- `AppK.S4.b`: `QuarantineCert` — Receipt proving quarantine was enforced within `Δt = 1`, listing all routing tables updated, metro lines cleared, and computation queues purged of the quarantined shard.
- `AppK.S4.c`: `NonProliferationCert` — Receipt proving no new conflicts were generated by the quarantine action, with a diff of the conflict ledger before and after quarantine showing only the original conflict and its `DependencyMissing` derivatives.
- `AppK.S4.d`: `RevocationReceipt` — Permanent record proving a shard was revoked: its address retired, content deleted, all references removed, and the cooldown period `T_cooldown` registered in the address registry.

### Lens F

#### Facet 1 - Objects

- `AppK.F1.a`: `ConflictAmplificationChain` — A dynamic sequence where conflict `C_1` at address `A_1` triggers conflict `C_2` at dependent address `A_2`, which triggers `C_3` at `A_3`, forming a causal chain of escalating contradictions that must be halted before reaching critical mass.
- `AppK.F1.b`: `ConflictCancellationPair` — Two conflicts that mutually neutralize: `C_+` (a positive deviation) and `C_-` (a negative deviation) at nearby addresses whose combined effect is zero. Detected by checking if `Δ_+(C_+) + Δ_-(C_-) = 0` within tolerance.
- `AppK.F1.c`: `QuarantineDecayFunction` — The time-dependent function `Q(t) = Q_0 · e^{-t/τ}` governing how quarantine severity relaxes over time as the conflict is investigated. Starts at full isolation `Q_0 = 1` and decays toward supervised access as evidence of resolution accumulates.
- `AppK.F1.d`: `ConflictWaveFront` — The expanding boundary of a conflict's influence zone, propagating at speed `v ≤ 1` shell per time step through the dependency graph. Shards inside the wave front are pre-quarantined; those outside are safe.

#### Facet 2 - Laws

- `AppK.F2.a`: `AmplificationCutoffLaw` — Conflict amplification chains are cut off at length `L_max = 6` (one per conservation law). If a chain reaches length 6 without resolution, the entire chain is escalated to organism-level crisis handling.
- `AppK.F2.b`: `CancellationSymmetryLaw` — Conflict cancellation is symmetric: if `C_+` cancels `C_-`, then `C_-` cancels `C_+`. The cancellation operator is commutative and produces a `CancellationReceipt` that retires both conflicts simultaneously.
- `AppK.F2.c`: `QuarantineDecayFloorLaw` — Quarantine severity never drops below `Q_min = 0.1` (10% isolation) until the conflict is formally resolved. Even the most relaxed quarantine maintains basic monitoring and prevents the shard from participating in safety-critical operations.
- `AppK.F2.d`: `WaveFrontContainmentLaw` — The conflict wave front must be contained within a finite radius `R_max` from the source. If the wave front threatens to exceed `R_max`, a firebreak is constructed by proactively quarantining the shards at radius `R_max`.

#### Facet 3 - Constructions

- `AppK.F3.a`: `AmplificationChainTracer` — The tracer that follows a conflict's causal chain through the dependency graph, recording each hop, measuring the amplification factor at each step, and triggering cutoff when chain length reaches `L_max`.
- `AppK.F3.b`: `CancellationDetector` — The detector that scans pairs of active conflicts for cancellation potential, computing the combined deviation `Δ_+ + Δ_-` and flagging pairs where the sum falls within tolerance `ε` of zero.
- `AppK.F3.c`: `QuarantineScheduler` — The scheduler that manages quarantine decay over time, computing `Q(t)` for each quarantined shard, adjusting access permissions as severity decreases, and triggering full release upon formal resolution.
- `AppK.F3.d`: `FirebreakConstructor` — The emergency construction that builds a quarantine perimeter at radius `R_max` from a conflict source, proactively isolating all shards on the perimeter to prevent wave front escape.

#### Facet 4 - Certificates

- `AppK.F4.a`: `AmplificationCutoffCert` — Receipt proving a conflict chain was traced and cut off at the legal maximum length, with the full chain recorded, amplification factors measured, and escalation triggered if applicable.
- `AppK.F4.b`: `CancellationCert` — Receipt proving two conflicts mutually cancelled, the combined deviation is within tolerance, both conflicts are retired, and the cancellation was symmetric.
- `AppK.F4.c`: `QuarantineDecayCert` — Receipt proving quarantine severity followed the legal decay function, never dropped below the floor, and the shard was released only upon formal conflict resolution.
- `AppK.F4.d`: `FirebreakCert` — Receipt proving a firebreak was constructed at the correct radius, all perimeter shards were quarantined, and the conflict wave front was contained within the declared boundary.

### Lens C

#### Facet 1 - Objects

- `AppK.C1.a`: `ConflictProbabilityField` — The probability field `P_conflict(addr)` assigning to each crystal address the estimated probability that a conflict will occur there within the next `T` time steps, computed from historical conflict rates and dependency structure.
- `AppK.C1.b`: `WeightedEvidenceBundle` — A collection of `n` evidence items `{(e_i, w_i)}` bearing on a conflict's resolution, where each item has a weight `w_i` proportional to its reliability. The weighted sum `Σ w_i · sign(e_i)` determines the resolution direction.
- `AppK.C1.c`: `BayesianConflictResolver` — The probabilistic resolver that updates a prior belief `P(innocent | prior)` with each evidence item via Bayes' rule: `P(innocent | e) ∝ P(e | innocent) · P(innocent)`, converging toward a resolution as evidence accumulates.
- `AppK.C1.d`: `VotingQuorum` — A conflict resolution mechanism where `k` independent verifiers each cast a vote `v_i ∈ {keep, revoke, defer}` and the majority decision (requiring `> k/2` agreement) determines the shard's fate.

#### Facet 2 - Laws

- `AppK.C2.a`: `ConflictProbabilityCalibration` — The conflict probability field must be calibrated: among all addresses assigned probability `p`, the observed fraction experiencing conflicts within `T` steps must lie within `p ± ε` for declared tolerance `ε`.
- `AppK.C2.b`: `EvidenceWeightNormalization` — Evidence weights must sum to 1 within each bundle: `Σ w_i = 1`. This prevents evidence inflation and ensures the Bayesian update is properly normalized.
- `AppK.C2.c`: `MajorityConvergenceLaw` — As the number of independent verifiers `k` grows, the probability that the majority vote reaches the correct resolution converges to 1, provided each verifier has individual accuracy `> 0.5` (Condorcet jury theorem).
- `AppK.C2.d`: `FalsePositiveBoundLaw` — The conflict detection system's false positive rate must not exceed `α_max`: at most `α_max` fraction of flagged conflicts may turn out to be non-conflicts upon investigation. Excessive false positives erode trust in the quarantine system.

#### Facet 3 - Constructions

- `AppK.C3.a`: `ConflictRiskEstimator` — The engine that computes the conflict probability field by combining historical conflict rates, dependency graph centrality measures, and current residual levels (from AppJ) into a per-address risk score.
- `AppK.C3.b`: `EvidenceWeighter` — The engine that assigns reliability weights to evidence items based on source credibility, temporal recency, and consistency with other evidence, normalizing to sum to 1.
- `AppK.C3.c`: `BayesianUpdater` — The sequential Bayesian updater that processes evidence items one at a time, maintaining the posterior probability of each resolution outcome, and declaring resolution when the posterior exceeds the decision threshold `τ`.
- `AppK.C3.d`: `QuorumAssembler` — The construction that selects `k` independent verifiers (shards not in the conflict's dependency cone), distributes the conflict evidence to each, collects votes, and computes the majority decision with confidence intervals.

#### Facet 4 - Certificates

- `AppK.C4.a`: `RiskCalibrationCert` — Receipt proving the conflict probability field is calibrated within tolerance `ε`, with the calibration curve, observed vs. predicted rates, and the estimation methodology recorded.
- `AppK.C4.b`: `EvidenceWeightCert` — Receipt proving evidence weights are normalized, each weight is justified by source credibility assessment, and the total weight equals 1.
- `AppK.C4.c`: `BayesianResolutionCert` — Receipt proving the Bayesian conflict resolution converged to a posterior above threshold `τ`, with the full evidence chain and each update step recorded.
- `AppK.C4.d`: `QuorumCert` — Receipt proving the voting quorum was properly assembled (all verifiers independent), votes were tallied correctly, and the majority decision exceeds the required agreement threshold.

### Lens R

#### Facet 1 - Objects

- `AppK.R1.a`: `SelfHealingContradiction` — A contradiction that contains its own resolution mechanism: when detected, the conflict packet includes a `repair` function derived from the contradiction's structure, which, when applied, resolves the inconsistency without external intervention.
- `AppK.R1.b`: `ConflictFixedPoint` — The stable state `C* = Resolve(C*)` where the conflict resolution process, applied to its own output, produces the same result. A conflict at its fixed point is fully resolved; the resolution is self-consistent.
- `AppK.R1.c`: `RecursiveQuarantineShell` — A quarantine zone that recursively quarantines its own boundary: if a quarantined shard's quarantine status creates a dependency conflict, that meta-conflict is itself quarantined, forming nested isolation shells to depth `D_max`.
- `AppK.R1.d`: `ContradictionEigenmode` — The eigenvector of the conflict interaction matrix `M` with eigenvalue `λ`: a pattern of contradictions that, when the system processes them, reproduces the same pattern scaled by `λ`. If `|λ| < 1`, the eigenmode decays (self-resolving); if `|λ| > 1`, it amplifies (requiring intervention).

#### Facet 2 - Laws

- `AppK.R2.a`: `SelfHealingConvergenceLaw` — Self-healing contradictions must converge: the embedded repair function is a contraction with `κ < 1`, guaranteeing resolution within `⌈log(δ_0/ε)/log(1/κ)⌉` iterations by Banach's theorem, mirroring AppJ's contraction law.
- `AppK.R2.b`: `FixedPointConsistencyLaw` — The conflict fixed point must be unique and consistent with all six conservation laws (AppB). A resolution that satisfies itself but violates conservation is not a true fixed point; it is a pseudo-resolution that will re-trigger detection.
- `AppK.R2.c`: `RecursiveQuarantineDepthBound` — Recursive quarantine nesting is bounded at depth `D_max = 6` (one per conservation law). Deeper nesting indicates a fundamental structural problem requiring organism-level intervention rather than local containment.
- `AppK.R2.d`: `EigenmodeStabilityLaw` — All contradiction eigenmodes must have `|λ| ≤ 1`: no amplifying eigenmode is permitted in the steady state. If `|λ| > 1` is detected, the corresponding contradiction pattern must be suppressed by targeted quarantine of the eigenmode's support.

#### Facet 3 - Constructions

- `AppK.R3.a`: `SelfHealingEngine` — The engine that executes a self-healing contradiction's embedded repair function iteratively, tracking convergence via `δ_n = |C_n - C_{n-1}|`, and declaring resolution when `δ_n < ε_target` for the first time.
- `AppK.R3.b`: `FixedPointResolver` — The resolver that computes the conflict fixed point by iterating `C_{n+1} = Resolve(C_n)` from the initial conflict state, verifying uniqueness by checking that different initial perturbations converge to the same `C*`.
- `AppK.R3.c`: `RecursiveQuarantineManager` — The manager that handles nested quarantine shells, tracking the depth of each, escalating when depth exceeds `D_max`, and collapsing inner shells as their conflicts resolve from inside out.
- `AppK.R3.d`: `EigenmodeAnalyzer` — The spectral analyzer that computes the conflict interaction matrix `M`, extracts its eigenvalues and eigenvectors, identifies any amplifying modes `|λ| > 1`, and generates targeted quarantine plans for their support.

#### Facet 4 - Certificates

- `AppK.R4.a`: `SelfHealingCert` — Receipt proving a self-healing contradiction resolved itself via its embedded repair function, the contraction constant `κ` was verified, and convergence occurred within the predicted iteration bound.
- `AppK.R4.b`: `FixedPointResolutionCert` — Receipt proving the conflict fixed point `C*` was found, is unique, is consistent with all six conservation laws, and was reached from the actual conflict state (not just constructed abstractly).
- `AppK.R4.c`: `RecursiveQuarantineCert` — Receipt proving recursive quarantine operated within the depth bound `D_max`, inner shells collapsed correctly as conflicts resolved, and no meta-conflict escaped containment.
- `AppK.R4.d`: `EigenmodeStabilityCert` — Receipt proving all contradiction eigenmodes have `|λ| ≤ 1`, the spectral analysis used the correct conflict interaction matrix, and any previously amplifying modes were successfully suppressed.
