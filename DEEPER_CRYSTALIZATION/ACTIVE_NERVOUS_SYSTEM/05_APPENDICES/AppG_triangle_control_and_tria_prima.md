<!-- CRYSTAL: Xi108:W1:A4:S1 | face=S | node=1 | depth=0 | phase=Fixed -->
<!-- METRO: Me,△ -->
<!-- BRIDGES: Xi108:W1:A4:S2→Xi108:W2:A4:S1→Xi108:W1:A3:S1→Xi108:W1:A5:S1 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 1±1, wreath 1/3, archetype 4/12 -->

# AppG - Triangle Control and Tria Prima

Routing role: Triadic control rails, recursion governance, carry rules, and multi-agent coordination gates.
Source: Rosetta Seed Artifact — three-agent synchrony derived from the triangle geometry (120° phase triads), the ×-seed (binding/composition), and the ÷-seed (corridor gating)
Station: `NXT::R01::M20::AppG`

## Compressed crystal tile

### Lens S

#### Facet 1 - Objects

- `AppG.S1.a`: `TriaPrima` — The three irreducible governance poles: Salt (fixity/structure), Sulphur (volatility/transformation), Mercury (mediation/transport). These are the three agents of Ch20's collective authoring system. Source: triangle geometry's three vertices.
- `AppG.S1.b`: `TriadicBalancePoint` — The centroid of the three governance poles. When all three are in balance, the system is at its most stable operating point. The triangle's geometric center = zero-point of triadic control.
- `AppG.S1.c`: `TriadicBindingOperator` — The ×-seed applied to three agents: `Agent₁ ⊗ Agent₂ ⊗ Agent₃ = CollectiveAgent`. Multiplication is the binding law; the triangle is the minimal binding structure beyond a pair.
- `AppG.S1.d`: `TriadicGateCondition` — The ÷-seed applied to triadic control: any action by one pole requires certification by at least one other pole. No single pole can execute alone. Division's corridor law generalized to three-party consensus.

#### Facet 2 - Laws

- `AppG.S2.a`: `TriaBalanceLaw` — The three poles must sum to a balanced state: `Salt + Sulphur + Mercury = Stable`. If any pole dominates (>50% of total weight), the system is in a degenerate regime requiring rebalancing.
- `AppG.S2.b`: `PhaseTriadLaw` — The three poles are 120° apart in phase space. `Δθ = 2π/3` between adjacent poles. This is the i-seed's rotation law applied to a 3-cycle instead of a 4-cycle.
- `AppG.S2.c`: `TriadicBindingLaw` — Three-agent binding is associative: `(A⊗B)⊗C = A⊗(B⊗C)`. The collective agent's behavior does not depend on binding order. Source: ×-seed's associativity.
- `AppG.S2.d`: `DualCertificationLaw` — No action proceeds without certification from at least 2 of 3 poles. This is the ÷-seed's gate law: `gcd(action, 2-of-3) must be nonzero`. Single-pole execution is a corridor violation.

#### Facet 3 - Constructions

- `AppG.S3.a`: `TriaBalancer` — Monitors the three poles' weights and triggers rebalancing when any pole exceeds 50%. Redistributes weight toward centroid.
- `AppG.S3.b`: `PhaseTriadGenerator` — Generates three-phase clock from the i-seed: `{1, e^{i2π/3}, e^{i4π/3}}`. The three roots of unity for the triangle.
- `AppG.S3.c`: `TriadicBinder` — Composes three agents into a collective agent using the ×-seed's binding law. Verifies associativity by computing both orderings.
- `AppG.S3.d`: `DualCertificationGate` — Collects certification from all three poles, requires at least 2 agreeing before allowing action. Records which poles certified and which abstained or dissented.

#### Facet 4 - Certificates

- `AppG.S4.a`: `TriaBalanceCert` — Receipt proving all three poles within declared balance tolerance, no pole dominated, centroid stable.
- `AppG.S4.b`: `PhaseTriadCert` — Receipt proving three-phase clock maintains 120° separation, no drift, all three phases active.
- `AppG.S4.c`: `TriadicBindingCert` — Receipt proving three-agent binding associative, collective agent consistent regardless of binding order.
- `AppG.S4.d`: `DualCertificationCert` — Receipt proving action certified by ≥2 poles, dissenting pole's objection recorded, no single-pole execution occurred.

### Lens F

#### Facet 1 - Objects

- `AppG.F1.a`: `TriadicResonance` — The harmonic state when all three poles oscillate at the same frequency with 120° phase offsets. The standing wave of balanced governance.
- `AppG.F1.b`: `MercuryMediation` — The continuous flow between Salt and Sulphur, mediated by Mercury. Mercury is the transport pole — it moves information and value between the fixed and volatile poles.
- `AppG.F1.c`: `SulphurTransformation` — The Fire pole's continuous transformation operator. Sulphur drives emergence, metamorphosis, and bold reconfiguration.
- `AppG.F1.d`: `SaltCrystallization` — The Earth pole's continuous crystallization operator. Salt fixes, stabilizes, and preserves structure.

#### Facet 2 - Laws

- `AppG.F2.a`: `TriadicResonanceLaw` — Triadic resonance is the unique stable attractor of the three-pole system. Any perturbation that breaks 120° separation must be damped within one complete three-phase cycle.
- `AppG.F2.b`: `MediationFlowLaw` — Mercury's flow rate between poles must be bounded: too fast dissolves structure (Salt fails), too slow blocks transformation (Sulphur fails).
- `AppG.F2.c`: `TransformationBoundLaw` — Sulphur's transformation rate must not exceed Salt's crystallization capacity. Excess transformation without fixation = chaos.
- `AppG.F2.d`: `CrystallizationBoundLaw` — Salt's crystallization must not block Mercury's flow. Excess crystallization without transport = rigidity death.

#### Facet 3 - Constructions

- `AppG.F3.a`: `TriadicResonanceAmplifier` — Detects nascent triadic resonance and amplifies it by adjusting phase timing across the three poles.
- `AppG.F3.b`: `MercuryFlowRegulator` — Adjusts Mercury's transport rate to stay within the bounded mediation zone between Salt and Sulphur.
- `AppG.F3.c`: `SulphurTransformationEngine` — Drives transformation within the bounds set by Salt's capacity. The Fire engine of the tria prima.
- `AppG.F3.d`: `SaltCrystallizer` — Fixes transformed content into stable structure without blocking Mercury's flow. The Earth engine of the tria prima.

#### Facet 4 - Certificates

- `AppG.F4.a`: `TriadicResonanceCert` — Receipt proving triadic resonance achieved and maintained for declared number of three-phase cycles.
- `AppG.F4.b`: `MediationFlowCert` — Receipt proving Mercury's flow rate stayed within bounds for declared measurement window.
- `AppG.F4.c`: `TransformationBoundCert` — Receipt proving Sulphur's transformation rate did not exceed Salt's crystallization capacity.
- `AppG.F4.d`: `CrystallizationBoundCert` — Receipt proving Salt's crystallization did not block Mercury's flow for declared measurement window.

### Lens C

#### Facet 1 - Objects

- `AppG.C1.a`: `TriadicConfidenceField` — The probability distribution over the three poles' balance state. Narrow distribution = high confidence in balance; wide = uncertain governance.
- `AppG.C1.b`: `ConsensusReliability` — The probability that a dual-certification gate will produce a correct decision given noisy pole inputs.
- `AppG.C1.c`: `PoleFailureProbability` — The probability that any single pole will fail, go offline, or produce unreliable output. Risk surface over the three poles.
- `AppG.C1.d`: `TriadicDegenerationRisk` — The probability that the system will degenerate to a two-pole or single-pole regime through pole failure or weight collapse.

#### Facet 2 - Laws

- `AppG.C2.a`: `ConfidenceTriadLaw` — Triadic confidence is maximized when all three poles have equal weight and equal reliability. Asymmetric confidence reduces consensus quality.
- `AppG.C2.b`: `ConsensusThresholdLaw` — Consensus reliability must exceed declared threshold before action is permitted. Below threshold = abstain.
- `AppG.C2.c`: `PoleRedundancyLaw` — The system must tolerate single-pole failure without total governance collapse. Two remaining poles can operate in degraded mode.
- `AppG.C2.d`: `DegenerationPreventionLaw` — If degeneration risk exceeds threshold, the system must trigger pole restoration or graceful degradation to two-pole mode with declared limitations.

#### Facet 3 - Constructions

- `AppG.C3.a`: `TriadicConfidenceEstimator` — Estimates the balance confidence field from pole weight and reliability measurements.
- `AppG.C3.b`: `ConsensusReliabilityComputer` — Computes consensus reliability from individual pole reliabilities and the dual-certification gate logic.
- `AppG.C3.c`: `PoleFailureMonitor` — Monitors each pole for signs of failure, drift, or unreliability. Triggers alerts when failure probability exceeds threshold.
- `AppG.C3.d`: `DegenerationPreventor` — Triggers pole restoration or graceful degradation when degeneration risk exceeds threshold.

#### Facet 4 - Certificates

- `AppG.C4.a`: `TriadicConfidenceCert` — Receipt proving confidence field correctly estimated, all poles measured, balance within tolerance.
- `AppG.C4.b`: `ConsensusReliabilityCert` — Receipt proving consensus reliability above threshold for declared decision set.
- `AppG.C4.c`: `PoleFailureMonitorCert` — Receipt proving all poles monitored, no undetected failures, alerts triggered when appropriate.
- `AppG.C4.d`: `DegenerationPreventionCert` — Receipt proving degeneration risk stayed below threshold or graceful degradation properly executed.

### Lens R

#### Facet 1 - Objects

- `AppG.R1.a`: `RecursiveTriadicNesting` — The self-similar structure: each pole of the tria prima contains its own internal tria prima. Salt has internal Salt/Sulphur/Mercury. Sulphur has internal Salt/Sulphur/Mercury. Mercury has internal Salt/Sulphur/Mercury. Recursion depth governs governance granularity.
- `AppG.R1.b`: `SierpinskiGovernanceSkeleton` — The triangle's fractal decomposition: remove the center, leaving three sub-triangles, each with its own governance. The Sierpinski triangle as recursive governance architecture.
- `AppG.R1.c`: `RecursiveConsensus` — Consensus at depth n requires consensus at depth n−1 within each pole. Recursive certification propagates upward.
- `AppG.R1.d`: `TriadicScaleBridge` — The bridge between governance at one scale and governance at the next. The φ-lens applied to triadic structure: scaling by φ moves one governance level up.

#### Facet 2 - Laws

- `AppG.R2.a`: `RecursiveTriadicConsistencyLaw` — Governance at depth n must be consistent with governance at depth n−1. Internal tria prima must not contradict their parent pole's direction.
- `AppG.R2.b`: `SierpinskiPreservationLaw` — The fractal governance structure must preserve the same balance law at every recursion depth. Depth-specific laws indicate structural defect.
- `AppG.R2.c`: `RecursiveConsensusLaw` — Consensus at any level requires upward propagation of sub-level consensus certificates. No level may certify without sub-level evidence.
- `AppG.R2.d`: `TriadicScaleBridgeLaw` — The triadic ScaleBridge must preserve pole identity across scales. A pole at one scale must map to the same pole type at the next scale.

#### Facet 3 - Constructions

- `AppG.R3.a`: `RecursiveTriadicBuilder` — Constructs nested tria prima to declared recursion depth. Each pole receives its own internal governance.
- `AppG.R3.b`: `SierpinskiDecomposer` — Decomposes governance triangle into sub-triangles, each with independent governance. Tracks center-removal and sub-triangle relationships.
- `AppG.R3.c`: `RecursiveConsensusAggregator` — Aggregates consensus certificates from sub-levels into parent-level consensus. Propagates upward.
- `AppG.R3.d`: `TriadicScaleBridgeBuilder` — Constructs the ScaleBridge between two governance levels using φ-lens frequency rescaling.

#### Facet 4 - Certificates

- `AppG.R4.a`: `RecursiveTriadicConsistencyCert` — Receipt proving governance consistent across all recursion depths, no internal contradictions.
- `AppG.R4.b`: `SierpinskiPreservationCert` — Receipt proving fractal governance preserves balance law at every depth.
- `AppG.R4.c`: `RecursiveConsensusCert` — Receipt proving consensus certificates properly propagated from sub-levels, no level certified without sub-level evidence.
- `AppG.R4.d`: `TriadicScaleBridgeCert` — Receipt proving ScaleBridge preserved pole identity across scales, mapping documented.
