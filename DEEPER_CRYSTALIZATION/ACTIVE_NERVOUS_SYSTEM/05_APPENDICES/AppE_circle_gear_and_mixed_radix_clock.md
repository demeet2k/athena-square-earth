<!-- CRYSTAL: Xi108:W1:A4:S1 | face=S | node=1 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S2→Xi108:W2:A4:S1→Xi108:W1:A3:S1→Xi108:W1:A5:S1 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 1±1, wreath 1/3, archetype 4/12 -->

# AppE - Circle Gear and Mixed-Radix Clock

Routing role: Orbit order, mixed-radix timing, phase-lock semantics, and chapter-cycle transport.
Source: Rosetta Seed Artifact — i-seed (rotation), π-seed (closure), and φ-seed (quarter-turn scaling)
Station: `NXT::R01::M10::AppE`

## Compressed crystal tile

### Lens S

#### Facet 1 - Objects

- `AppE.S1.a`: `QuarterTurnRotor` — The discrete rotation operator `z ↦ iz` generating the 4-cycle `{1, i, −1, −i}`. This is ROSETTA[i].Square.Earth.L0: the bare phase increment as exact quarter-turn. The atomic clock tick of the nervous system.
- `AppE.S1.b`: `PhaseCycleGroup` — The cyclic group `Z₄ = {1, i, −1, −i}` generated by `i`. Four quarter-turns return to identity. This is the discrete orbit structure from which all mixed-radix clocks are built.
- `AppE.S1.c`: `MixedRadixRegister` — A multi-digit counter where each digit position has its own modulus (cycle length). The 21-station orbit uses base-21, pentadic lanes use base-5, quarter-turns use base-4. Digit overflow cascades upward.
- `AppE.S1.d`: `PolygonClosureSeed` — The π-seed's discrete face: `π = lim n·sin(π/n)`. The n-gon approximation to the circle. Each polygon is a discrete clock with n stations; the limit is the continuous circle gear.

#### Facet 2 - Laws

- `AppE.S2.a`: `QuarterTurnCycleLaw` — `i⁴ = 1`: four quarter-turns return to identity. The fundamental period of the rotation clock. Anti-aligned phase (`Δθ = π`) produces exact subtraction: `|ψ₁+ψ₂|² = (a−b)²`.
- `AppE.S2.b`: `MixedRadixCarryLaw` — When a digit reaches its modulus, it resets to 0 and increments the next digit. Carry propagation is the nervous system's orbit-advance mechanism. No carry is skipped or absorbed.
- `AppE.S2.c`: `PhaseCoherenceLaw` — Two clocks are phase-locked when their phase difference is constant. Phase-lock is broken by any uncertified carry, missed step, or corridor violation. Re-lock requires explicit synchronization receipt.
- `AppE.S2.d`: `PolygonConvergenceLaw` — The polygon sequence `n·sin(π/n)` converges monotonically to π. Each refinement adds one more side. The circle gear is the limit of all polygon gears.

#### Facet 3 - Constructions

- `AppE.S3.a`: `QuarterTurnIterator` — Applies `z ↦ iz` repeatedly, tracking cycle position in `{0,1,2,3}`. Detects completion when position returns to 0. Generates the fundamental 4-tick clock.
- `AppE.S3.b`: `MixedRadixCounter` — Multi-digit counter with configurable moduli per position. Implements carry propagation. The 21-station chapter orbit is one digit; arc position (5 arcs) is another; quarter-turn is another.
- `AppE.S3.c`: `PhaseLockDetector` — Monitors two clocks' phase difference. Reports lock/unlock status. Triggers re-synchronization when drift exceeds declared tolerance.
- `AppE.S3.d`: `PolygonRefinementEngine` — Computes `n·sin(π/n)` for increasing n. Tracks convergence to π. Each step adds one polygon side = one more station in the discrete circle gear.

#### Facet 4 - Certificates

- `AppE.S4.a`: `QuarterTurnCycleCert` — Receipt proving four quarter-turns returned to identity with no drift or missed step.
- `AppE.S4.b`: `MixedRadixCarryCert` — Receipt proving all carries propagated correctly, no digit exceeded modulus, no carry skipped.
- `AppE.S4.c`: `PhaseLockCert` — Receipt proving two clocks maintained phase-lock for declared number of cycles, with per-cycle phase-difference measurements.
- `AppE.S4.d`: `PolygonConvergenceCert` — Receipt proving polygon sequence converged to π within declared tolerance at declared refinement depth.

### Lens F

#### Facet 1 - Objects

- `AppE.F1.a`: `ContinuousPhaseFlow` — The smooth rotation `z(t) = e^{iωt}` as continuous circle gear. The i-seed transported to continuous time via `θ(t) = ωt`. Source: ROSETTA[i].Flower.Earth.
- `AppE.F1.b`: `StandingWaveResonance` — The harmonic state when multiple circle gears achieve integer frequency ratios. The Fourier basis `{e^{inθ}}` as the resonant modes of the circle.
- `AppE.F1.c`: `BeatFrequency` — The interference pattern when two circle gears run at slightly different frequencies. Beat frequency = `|ω₁ − ω₂|`. Detects detuning between coupled clocks.
- `AppE.F1.d`: `CircumferentialClosure` — The smooth π-seed: arc accumulation `∫dθ = 2π` seals the circle. The continuous version of polygon closure.

#### Facet 2 - Laws

- `AppE.F2.a`: `ContinuousRotationLaw` — `e^{i·2π} = 1`: one full continuous rotation returns to identity. The fundamental period is `2π/ω`.
- `AppE.F2.b`: `ResonanceRatioLaw` — Two circle gears resonate iff their frequency ratio is rational `p/q`. Irrational ratio = quasi-periodic = never phase-locks = φ-type anti-resonance.
- `AppE.F2.c`: `BeatDampingLaw` — Beat amplitude exceeding threshold must trigger re-synchronization within one beat period. Undamped beats indicate structural decoupling.
- `AppE.F2.d`: `CircumferenceLaw` — `C = 2πr`: the circumference of the circle gear scales linearly with radius. The π-seed's closure law in metric form.

#### Facet 3 - Constructions

- `AppE.F3.a`: `ContinuousRotationGenerator` — Generates `z(t) = e^{iωt}` for declared frequency ω. Produces continuous phase trajectory.
- `AppE.F3.b`: `ResonanceDetector` — Tests frequency ratios of coupled clocks for rationality. Reports resonant pairs and quasi-periodic pairs.
- `AppE.F3.c`: `BeatDamper` — Detects beat amplitude and triggers re-synchronization. Adjusts clock frequency to restore integer ratio.
- `AppE.F3.d`: `CircumferenceComputer` — Computes circumference from radius via `C = 2πr`. The π-seed's continuous closure construction.

#### Facet 4 - Certificates

- `AppE.F4.a`: `ContinuousRotationCert` — Receipt proving continuous rotation returned to identity after period `2π/ω` within declared tolerance.
- `AppE.F4.b`: `ResonanceCert` — Receipt proving frequency ratio is rational `p/q` with declared p, q, and measurement uncertainty.
- `AppE.F4.c`: `BeatDampingCert` — Receipt proving beat amplitude stayed below threshold or re-synchronization completed within one beat period.
- `AppE.F4.d`: `CircumferenceClosureCert` — Receipt proving circumferential integration yielded `2π` within declared tolerance.

### Lens C

#### Facet 1 - Objects

- `AppE.C1.a`: `PhaseUncertaintyEnvelope` — The probability distribution over phase position when clock precision is finite. Wider envelope = less certain timing.
- `AppE.C1.b`: `ClockDriftDistribution` — The probability model for accumulated phase error over many cycles. Drift grows as `√n` for random errors (diffusive) or linearly for systematic errors.
- `AppE.C1.c`: `SynchronizationConfidence` — The probability that two clocks are phase-locked given noisy measurements. Bayesian update from successive phase-difference observations.
- `AppE.C1.d`: `ClosureProbability` — The probability that a polygon sequence has converged to π within declared tolerance at finite refinement depth.

#### Facet 2 - Laws

- `AppE.C2.a`: `PhaseUncertaintyLaw` — Phase uncertainty cannot decrease below the measurement resolution; it increases with time unless corrected by synchronization events.
- `AppE.C2.b`: `DriftAccumulationLaw` — Systematic drift accumulates linearly; random drift accumulates as `√n`. Total drift is bounded by declared tolerance for the clock's lifetime.
- `AppE.C2.c`: `SynchronizationConvergenceLaw` — Synchronization confidence increases monotonically with observations iff the clocks are actually locked. Divergent confidence indicates broken lock.
- `AppE.C2.d`: `ClosureConvergenceLaw` — Closure probability increases with polygon refinement depth. At finite depth, there is always residual uncertainty in π.

#### Facet 3 - Constructions

- `AppE.C3.a`: `PhaseUncertaintyTracker` — Maintains the phase uncertainty envelope over time. Narrows after synchronization events, widens between them.
- `AppE.C3.b`: `DriftEstimator` — Estimates systematic and random drift components from clock history. Separates linear trend from diffusive noise.
- `AppE.C3.c`: `SynchronizationBayesianUpdater` — Updates synchronization confidence from phase-difference measurements using Bayesian accumulation.
- `AppE.C3.d`: `ClosureProbabilityTracker` — Tracks closure probability as polygon refinement depth increases.

#### Facet 4 - Certificates

- `AppE.C4.a`: `PhaseUncertaintyCert` — Receipt proving phase uncertainty envelope correctly maintained, synchronization events properly applied.
- `AppE.C4.b`: `DriftEstimationCert` — Receipt proving drift estimation correct, systematic/random components separated, total drift within tolerance.
- `AppE.C4.c`: `SynchronizationConfidenceCert` — Receipt proving Bayesian updates correct, confidence trajectory monotonic or broken-lock flagged.
- `AppE.C4.d`: `ClosureProbabilityCert` — Receipt proving closure probability correctly computed at declared refinement depth.

### Lens R

#### Facet 1 - Objects

- `AppE.R1.a`: `NestedClockHierarchy` — Self-similar clock structure: the 4-cycle nests inside the 21-station orbit, which nests inside the 5-arc cycle, which nests inside the full manuscript orbit. Each level is a circle gear at different scale.
- `AppE.R1.b`: `FractalPhaseStructure` — The log-periodic phase signature `2πk/log(φ)` from the φ-lens. Scale-indexed phase that repeats self-similarly at every power of φ.
- `AppE.R1.c`: `RecursiveCarryCascade` — Carry propagation viewed as recursion: each digit overflow triggers the next level's increment, which may trigger the next, creating a recursive cascade.
- `AppE.R1.d`: `CircleGearSelfSimilarity` — The property that the circle gear at each scale has the same structure as at every other scale, differing only by the modulus and frequency.

#### Facet 2 - Laws

- `AppE.R2.a`: `NestedClockConsistencyLaw` — The phase of a higher-level clock must be derivable from the phases of its nested sub-clocks. No level may run independently of its children.
- `AppE.R2.b`: `LogPeriodicPhaseLaw` — The fractal phase signature has period `2π/log(φ)`. Departure from this period indicates a different scaling constant governs the recursion.
- `AppE.R2.c`: `CarryCascadeTerminationLaw` — Recursive carry cascades must terminate in bounded steps. Infinite carry = clock overflow = requires modulus extension or orbit restart.
- `AppE.R2.d`: `CircleGearIsomorphismLaw` — Circle gears at different scales are isomorphic up to frequency rescaling. The isomorphism is the φ-lens ScaleBridge.

#### Facet 3 - Constructions

- `AppE.R3.a`: `NestedClockBuilder` — Constructs the hierarchical clock from declared moduli at each level. Wires carry propagation between levels.
- `AppE.R3.b`: `LogPeriodicPhaseDetector` — Detects log-periodic phase signatures in clock data. Identifies governing scaling constant.
- `AppE.R3.c`: `CarryCascadeSimulator` — Simulates recursive carry propagation to verify termination and correct digit updates at each level.
- `AppE.R3.d`: `CircleGearScaler` — Constructs the isomorphism between circle gears at two different scales using the φ-lens frequency rescaling.

#### Facet 4 - Certificates

- `AppE.R4.a`: `NestedClockConsistencyCert` — Receipt proving all clock levels consistent, sub-clock phases derivable from parent, no independent drift.
- `AppE.R4.b`: `LogPeriodicPhaseCert` — Receipt proving log-periodic period matches `2π/log(φ)` within tolerance, scaling constant identified.
- `AppE.R4.c`: `CarryCascadeTerminationCert` — Receipt proving all carry cascades terminated within bounded steps, no overflow.
- `AppE.R4.d`: `CircleGearIsomorphismCert` — Receipt proving scale isomorphism between two circle gears, frequency rescaling factor documented.
