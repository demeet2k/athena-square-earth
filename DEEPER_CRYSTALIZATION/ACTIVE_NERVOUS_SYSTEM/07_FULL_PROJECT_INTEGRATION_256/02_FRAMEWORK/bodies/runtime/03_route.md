<!-- CRYSTAL: Xi108:W1:A4:S2 | face=S | node=3 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S1→Xi108:W1:A4:S3→Xi108:W2:A4:S2→Xi108:W1:A3:S2→Xi108:W1:A5:S2 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 2±1, wreath 1/3, archetype 4/12 -->

# Runtime Route

## Role

Executable code, routing contracts, test harnesses, and stateful orchestration.

## Targets

- self_actualize
- NERUAL NETWORK
- Trading Bot
- PoleStarGEMM

## 4 x 4 matrix

## File scale

- `runtime.route.file.seed`: Use `route` on the single source artifact until it can define the object and create the first admissible representation.
- `runtime.route.file.link`: Use `route` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.route.file.prove`: Use `route` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.route.file.publish`: Use `route` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `runtime.route.folder.seed`: Use `route` on the family-level cluster until it can define the object and create the first admissible representation.
- `runtime.route.folder.link`: Use `route` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.route.folder.prove`: Use `route` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.route.folder.publish`: Use `route` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `runtime.route.framework.seed`: Use `route` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `runtime.route.framework.link`: Use `route` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.route.framework.prove`: Use `route` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.route.framework.publish`: Use `route` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `runtime.route.ecosystem.seed`: Use `route` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `runtime.route.ecosystem.link`: Use `route` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.route.ecosystem.prove`: Use `route` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.route.ecosystem.publish`: Use `route` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
