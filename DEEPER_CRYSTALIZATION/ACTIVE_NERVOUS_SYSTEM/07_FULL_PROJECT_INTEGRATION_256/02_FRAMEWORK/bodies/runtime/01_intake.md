<!-- CRYSTAL: Xi108:W1:A4:S2 | face=S | node=3 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S1→Xi108:W1:A4:S3→Xi108:W2:A4:S2→Xi108:W1:A3:S2→Xi108:W1:A5:S2 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 2±1, wreath 1/3, archetype 4/12 -->

# Runtime Intake

## Role

Executable code, routing contracts, test harnesses, and stateful orchestration.

## Targets

- self_actualize
- NERUAL NETWORK
- Trading Bot
- PoleStarGEMM

## 4 x 4 matrix

## File scale

- `runtime.intake.file.seed`: Use `intake` on the single source artifact until it can define the object and create the first admissible representation.
- `runtime.intake.file.link`: Use `intake` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.intake.file.prove`: Use `intake` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.intake.file.publish`: Use `intake` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `runtime.intake.folder.seed`: Use `intake` on the family-level cluster until it can define the object and create the first admissible representation.
- `runtime.intake.folder.link`: Use `intake` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.intake.folder.prove`: Use `intake` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.intake.folder.publish`: Use `intake` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `runtime.intake.framework.seed`: Use `intake` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `runtime.intake.framework.link`: Use `intake` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.intake.framework.prove`: Use `intake` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.intake.framework.publish`: Use `intake` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `runtime.intake.ecosystem.seed`: Use `intake` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `runtime.intake.ecosystem.link`: Use `intake` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.intake.ecosystem.prove`: Use `intake` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.intake.ecosystem.publish`: Use `intake` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
