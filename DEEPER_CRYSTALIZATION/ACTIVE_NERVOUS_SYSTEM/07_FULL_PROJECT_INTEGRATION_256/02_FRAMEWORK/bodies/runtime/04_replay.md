<!-- CRYSTAL: Xi108:W1:A4:S5 | face=S | node=12 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S4→Xi108:W1:A4:S6→Xi108:W2:A4:S5→Xi108:W1:A3:S5→Xi108:W1:A5:S5 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 5±1, wreath 1/3, archetype 4/12 -->

# Runtime Replay

## Role

Executable code, routing contracts, test harnesses, and stateful orchestration.

## Targets

- self_actualize
- NERUAL NETWORK
- Trading Bot
- PoleStarGEMM

## 4 x 4 matrix

## File scale

- `runtime.replay.file.seed`: Use `replay` on the single source artifact until it can define the object and create the first admissible representation.
- `runtime.replay.file.link`: Use `replay` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.replay.file.prove`: Use `replay` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.replay.file.publish`: Use `replay` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `runtime.replay.folder.seed`: Use `replay` on the family-level cluster until it can define the object and create the first admissible representation.
- `runtime.replay.folder.link`: Use `replay` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.replay.folder.prove`: Use `replay` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.replay.folder.publish`: Use `replay` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `runtime.replay.framework.seed`: Use `replay` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `runtime.replay.framework.link`: Use `replay` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.replay.framework.prove`: Use `replay` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.replay.framework.publish`: Use `replay` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `runtime.replay.ecosystem.seed`: Use `replay` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `runtime.replay.ecosystem.link`: Use `replay` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `runtime.replay.ecosystem.prove`: Use `replay` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `runtime.replay.ecosystem.publish`: Use `replay` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
