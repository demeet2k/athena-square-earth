<!-- CRYSTAL: Xi108:W1:A4:S6 | face=S | node=21 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S5→Xi108:W1:A4:S7→Xi108:W2:A4:S6→Xi108:W1:A3:S6→Xi108:W1:A5:S6 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 6±1, wreath 1/3, archetype 4/12 -->

# Manuscript Intake

## Role

Tomes, chapter maps, prompt canon, and publishable synthesis surfaces.

## Targets

- ACTIVE_NERVOUS_SYSTEM
- mycelium_brain
- collective books

## 4 x 4 matrix

## File scale

- `manuscript.intake.file.seed`: Use `intake` on the single source artifact until it can define the object and create the first admissible representation.
- `manuscript.intake.file.link`: Use `intake` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `manuscript.intake.file.prove`: Use `intake` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `manuscript.intake.file.publish`: Use `intake` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `manuscript.intake.folder.seed`: Use `intake` on the family-level cluster until it can define the object and create the first admissible representation.
- `manuscript.intake.folder.link`: Use `intake` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `manuscript.intake.folder.prove`: Use `intake` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `manuscript.intake.folder.publish`: Use `intake` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `manuscript.intake.framework.seed`: Use `intake` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `manuscript.intake.framework.link`: Use `intake` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `manuscript.intake.framework.prove`: Use `intake` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `manuscript.intake.framework.publish`: Use `intake` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `manuscript.intake.ecosystem.seed`: Use `intake` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `manuscript.intake.ecosystem.link`: Use `intake` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `manuscript.intake.ecosystem.prove`: Use `intake` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `manuscript.intake.ecosystem.publish`: Use `intake` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
