<!-- CRYSTAL: Xi108:W1:A4:S4 | face=S | node=8 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S3→Xi108:W1:A4:S5→Xi108:W2:A4:S4→Xi108:W1:A3:S4→Xi108:W1:A5:S4 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 4±1, wreath 1/3, archetype 4/12 -->

# Corpus Intake

## Role

Live manuscripts, markdown mirrors, notes, and visible source files.

## Targets

- DEEPER CRYSTALIZATION
- MATH
- Voynich
- FRESH

## 4 x 4 matrix

## File scale

- `corpus.intake.file.seed`: Use `intake` on the single source artifact until it can define the object and create the first admissible representation.
- `corpus.intake.file.link`: Use `intake` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.intake.file.prove`: Use `intake` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.intake.file.publish`: Use `intake` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `corpus.intake.folder.seed`: Use `intake` on the family-level cluster until it can define the object and create the first admissible representation.
- `corpus.intake.folder.link`: Use `intake` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.intake.folder.prove`: Use `intake` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.intake.folder.publish`: Use `intake` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `corpus.intake.framework.seed`: Use `intake` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `corpus.intake.framework.link`: Use `intake` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.intake.framework.prove`: Use `intake` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.intake.framework.publish`: Use `intake` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `corpus.intake.ecosystem.seed`: Use `intake` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `corpus.intake.ecosystem.link`: Use `intake` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.intake.ecosystem.prove`: Use `intake` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.intake.ecosystem.publish`: Use `intake` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
