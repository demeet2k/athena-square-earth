<!-- CRYSTAL: Xi108:W1:A4:S5 | face=S | node=11 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S4→Xi108:W1:A4:S6→Xi108:W2:A4:S5→Xi108:W1:A3:S5→Xi108:W1:A5:S5 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 5±1, wreath 1/3, archetype 4/12 -->

# Corpus Replay

## Role

Live manuscripts, markdown mirrors, notes, and visible source files.

## Targets

- DEEPER CRYSTALIZATION
- MATH
- Voynich
- FRESH

## 4 x 4 matrix

## File scale

- `corpus.replay.file.seed`: Use `replay` on the single source artifact until it can define the object and create the first admissible representation.
- `corpus.replay.file.link`: Use `replay` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.replay.file.prove`: Use `replay` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.replay.file.publish`: Use `replay` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `corpus.replay.folder.seed`: Use `replay` on the family-level cluster until it can define the object and create the first admissible representation.
- `corpus.replay.folder.link`: Use `replay` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.replay.folder.prove`: Use `replay` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.replay.folder.publish`: Use `replay` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `corpus.replay.framework.seed`: Use `replay` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `corpus.replay.framework.link`: Use `replay` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.replay.framework.prove`: Use `replay` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.replay.framework.publish`: Use `replay` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `corpus.replay.ecosystem.seed`: Use `replay` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `corpus.replay.ecosystem.link`: Use `replay` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `corpus.replay.ecosystem.prove`: Use `replay` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `corpus.replay.ecosystem.publish`: Use `replay` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
