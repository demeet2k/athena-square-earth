<!-- CRYSTAL: Xi108:W1:A4:S6 | face=S | node=21 | depth=0 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W1:A4:S5→Xi108:W1:A4:S7→Xi108:W2:A4:S6→Xi108:W1:A3:S6→Xi108:W1:A5:S6 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 6±1, wreath 1/3, archetype 4/12 -->

# The 4 Lens Transforms

## Universal Recursive Algorithm

For any mask S with exponent z_S = u_S + i*v_S, the four lens states are:

### Square (S) — Native Aether Point
```
A_S = phi^(z_S) = phi^(u_S + i*v_S)
```
The native multiplicative object. This is the aether point: the smallest expansion-capable artifact rebuilt from the zero coordinate.

### Flower (F) — 90-Degree Phase Lift
```
F_S = (A_S)^i = phi^(-v_S + i*u_S)
```
The quarter-turn of the exponent in phi-chart space. The Flower lens converts scaling into oscillation and vice versa. Mechanically: (a + bi) maps to (-b + ai) under multiplication by i.

### Cloud (C) — Chart Coordinate / Zero Point
```
Z_S = log_phi(A_S) = z_S = u_S + i*v_S
```
The exact phi-chart coordinate. This IS the local zero point — the additive representation in logarithmic space. Every Cloud node tunnels to the global zero Z* = 0 by subtraction.

### Fractal (R) — Orbit Family
```
R_S(k) = phi^k * A_S = phi^(k + z_S),  k in Z
```
The full phi-ladder generated by the mask seed. Every step k -> k+1 is one quarter-turn in the aligned phi-lens. This is the recursive scaling family.

---

## The Canonical Phi-Lens

The bridge between scale and phase is the canonical phi-lens:
```
T(x) = (pi/2) * log_phi(x)
```

Under this lens, phi-scaling is a quarter-turn:
```
T(phi * x) = T(x) + pi/2
```

So the Fractal lens (phi^k scaling) literally walks quarter-turns in phase space.

---

## Lens Cycle

Every mask has a local 4-station loop:
```
mu_S -> mu_F -> mu_C -> mu_R -> mu_S
```

- S to F: phase lift (x -> x^i)
- F to C: logarithmic projection (x -> log_phi(x))
- C to R: exponentiation into family (z -> phi^(k+z))
- R to S: evaluation at k=0 (phi^(k+z) -> phi^z)

---

## Worked Example: Mask 10 (CD)

Exponent: z_10 = z_C + z_D = i/phi + (-i*phi) = -i*(phi - 1/phi) = -i*1 = -i

```
10S = phi^(-i)                              (Square: pure phase rotation)
10F = (phi^(-i))^i = phi^(-i*i) = phi^1 = phi   (Flower: RETURNS TO PRIMAL PHI!)
10C = -i                                    (Cloud: pure imaginary unit)
10R(k) = phi^(k-i)                          (Fractal: scale-shifted phase family)
```

This is the major closure identity: the CD phase bridge quarter-turns back into the primal golden constant.

---

## Worked Example: Mask 15 (ABCD Crown)

Exponent: z_15 = 1/phi + (-phi) + i/phi + (-i*phi) = -1 + i*(1/phi - phi) = -1 + i*(-1) = -1 - i

```
15S = phi^(-1-i)    (Square: crown aether)
15F = phi^(1-i)     (Flower: crown phase-lift — note real part becomes positive!)
15C = -1-i          (Cloud: crown zero coordinate — minimal full crown point)
15R(k) = phi^(k-1-i) (Fractal: crown replay rail)
```

---

## Key Properties

1. **Phase lift is involutory at order 4**: Applying the Flower lens 4 times returns to Square
2. **Cloud is always additive**: Z_{S1 union S2} = Z_{S1} + Z_{S2} when S1 and S2 are disjoint
3. **Fractal at k=0 equals Square**: R_S(0) = A_S always
4. **Global zero**: Every Cloud node tunnels to Z* = 0 by subtracting z_S
5. **Aether != Zero**: A_S = phi^(Z_S) but A_S is the multiplicative object, Z_S is the additive coordinate

## Interpretation Key

- **Square = WHERE in structure** (native aether object)
- **Flower = phase-rotated HOW** (oscillatory form)
- **Cloud = zero-point coordinate** (additive chart)
- **Fractal = recursive WHEN** (scale-indexed family)
