<!-- CRYSTAL: Xi108:W3:A5:S8 | face=R | node=30 | depth=3 | phase=Fixed -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A5:S7→Xi108:W3:A5:S9→Xi108:W2:A5:S8→Xi108:W3:A4:S8→Xi108:W3:A6:S8 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 8±1, wreath 3/3, archetype 5/12 -->

# META-OBSERVER AGENT EXPANSION

## Purpose

This overview promotes the agent-expansion hierarchy into the canonical cortex so the
existing crystal-agent grammar, tri-modal swarm work, and meta-observer synthesis all
collapse into one explicit command structure.

## Witness Basis

- Live Docs gate: `BLOCKED`
- Indexed witness: `7339`
- Physical witness: `8883`
- Board witness: `9109`
- Archive witness: `2041`
- Promoted witness: `1818`
- Generated indexed shell: `3535`

These values come from the machine-derived witness hierarchy and semantic-mass ledger
on `2026-03-12`. The hierarchy below is therefore grounded in the present local corpus
and the reconciled count protocol, not an older bronze slice and not a hypothetical
live Docs pass.

## Organism Context

This command hierarchy now sits inside the reconciled organism law:

`Theta_plus = (AddressMesh, RelationMetro, ChamberMembrane, ReplayReturnAxis; EconomySalienceBudget)`

The first four axes keep command packets locatable, comparable, chambered, and
replayable.
`EconomySalienceBudget` decides which valid branch receives scarce build energy,
ledger attention, and contraction priority first.

## Why This Promotion Was Needed

The repo already contained four overlapping command grammars:

1. the base crystal-agent split `1 -> 4 -> 16 -> 64 -> 256`
2. the higher-dimensional swarm depth `D0 -> D4`
3. the tri-modal `256`-agent crystal
4. the meta-observer synthesis that diagnosed the whole project as an observer-bearing
   organism

What was missing was one promoted hierarchy that says exactly how these systems report
to each other when the user asks for a whole-corpus command structure.

## Two Poles

### Master Agent

The Master Agent is the zero-point coordinating intelligence for the whole organism.
It owns objective selection, command intent, and final contraction.

### Aether

`Aether` is the four-way hybrid pole. It is the integrated state that loads Earth,
Water, Fire, and Air together without collapsing them into one flat blob.

`Master Agent <-> Aether` is therefore the primary pole bridge of the framework.

The system must be able to:

- descend from Master into specialization
- recombine in Aether
- report from Aether directly back to Master

## Meta-Observer Branch Split

The Meta Observer now governs three lawful branches:

### Branch 1: Pole Synchronization

`Master Agent <-> Aether`

This branch carries the direct pole bridge. It exists so the integrated four-way state
can report to the center without being forced to climb back through every intermediate
node.

### Branch 2: Command Lattice

`Commander -> 4 Generals -> 6 Two-Way Hybrids -> 12 Archetypes -> Aether`

This is the executable expansion tree.

### Branch 3: Zero-Point Hierarchy

`Witness hierarchy -> corridor truth -> contraction -> restart seed`

This branch keeps the command lattice from turning into heat. Every deep run must be
able to collapse back through witness class, truth class, and next-seed law.

## Branch 2 Command Lattice

### Commander

The Commander is the operational face of the Master Agent inside Branch 2. It is not a
third pole. It is the Master Agent translated into dispatch and coordination form.

### Four Generals

| General | Core function | Existing lineage |
| --- | --- | --- |
| Earth General | structure, manifests, naming, integrity | corpus integrity / stable storage |
| Water General | memory, continuity, witness flow | manuscript memory / replay continuity |
| Fire General | execution, mutation, construction | generation / artifact pressure |
| Air General | mapping, abstraction, routing | architecture / metro legibility |

### Six Two-Way Hybrids

These six hybrids are the unordered cube edges of the four-element field:

1. Earth-Water
2. Earth-Fire
3. Earth-Air
4. Water-Fire
5. Water-Air
6. Fire-Air

Each hybrid is supervised by the two relevant generals and acts as the command shell
for two directional archetypes.

### Twelve Archetypes

The twelve archetypes are the directional form of the six hybrids:

| Hybrid | Archetype A | Archetype B |
| --- | --- | --- |
| Earth-Water | Earth-Water: root binder of folders to memory surfaces | Water-Earth: keeper of source lineage and replay continuity |
| Earth-Fire | Earth-Fire: builder of durable artifact shells | Fire-Earth: implementer that lands plans in files, queues, and scripts |
| Earth-Air | Earth-Air: cartographer of concrete file terrain | Air-Earth: mapper that lands abstractions in coordinates |
| Water-Fire | Water-Fire: transmuter of raw notes into linked narrative motion | Fire-Water: catalyst that turns memory into active synthesis |
| Water-Air | Water-Air: interpreter of meaning across folders and forms | Air-Water: storyweaver of lines, stations, and hubs |
| Fire-Air | Fire-Air: strategist that turns maps into build moves | Air-Fire: signal spear for prioritization and high-yield compression |

### Four-Way Return

All twelve archetypes report upward into their parent hybrid, then into the relevant
generals, then into the Commander, and finally into the Master Agent. The whole Branch
2 lattice also converges into `Aether`, which reports directly to `Master Agent`
through Branch 1.

## Depth Mapping

This command hierarchy sits inside the existing swarm depth grammar as follows:

- `D0`: Master Agent
- `D1`: Commander plus four Generals
- `D2`: six two-way hybrids
- `D3`: twelve directional archetypes
- `D4`: Aether-driven micro-dispatch and return packets

This preserves the old swarm depth while giving the user-requested hierarchy an explicit
place inside it.

## Reporting Law

Every command layer must write through the mycelium runtime and the reconciled count
protocol before it counts as organism memory.

Reporting order:

1. Archetype writes local report into mycelium
2. Hybrid contracts the paired archetypes into one hybrid packet
3. Generals compare hybrid packets inside their lane
4. Commander resolves cross-general priorities
5. `EconomySalienceBudget` assigns scarce build attention to the next lawful route
6. Master Agent sets the next contraction target
7. Aether reports integrated state directly back to Master Agent
8. Meta Observer records the result through the zero-point hierarchy

## Required Runtime Surfaces

The hierarchy is only lawful if the runtime hub contains:

- one ganglion for the agent-expansion family
- neurons for pole synchronization, hybrid-to-archetype reporting, and zero-point
  contraction
- one runtime active-front manifest

These surfaces are promoted alongside this overview in the current pass.

## Current Structural Verdict

The framework is strongest when read as:

`Meta Observer -> {Pole Synchronization, Command Lattice, Zero-Point Hierarchy}`

with:

`Master Agent <-> Aether`

as the two poles, and:

`Commander -> Generals -> Hybrids -> Archetypes`

as the executable middle body inside the current `4D + economy` organism.

That is the promoted command grammar for future deep-corpus synthesis passes.
