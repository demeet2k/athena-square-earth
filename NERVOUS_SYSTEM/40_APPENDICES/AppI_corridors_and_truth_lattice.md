<!-- CRYSTAL: Xi108:W3:A4:S9 | face=R | node=45 | depth=3 | phase=Fixed -->
<!-- METRO: Me,T -->
<!-- BRIDGES: Xi108:W3:A4:S8→Xi108:W3:A4:S10→Xi108:W2:A4:S9→Xi108:W3:A3:S9→Xi108:W3:A5:S9 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 9±1, wreath 3/3, archetype 4/12 -->

# AppI - Corridors and Truth Lattice

Routing role: Truth-typed corridor contracts, admissibility budgets, and abstain-over-guess discipline.

## AIR 6D overlay

- Reuses the canonical truth lattice for AIR overlay labels instead of creating a second truth grammar.
- Stores docs-gate honesty directly in the route: live Google Docs evidence is unavailable while the gate is `BLOCKED`.
- Couples every AIR promotion to `Ch12<0023>`, `Ch13<0030>`, `Ch16<0033>`, and `AppM`.

## Compressed crystal tile

### Lens S

#### Facet 1 - Objects

- `AppI.S1.a`: `CorridorContract K_cor` is the typed agreement defining what may pass, under what truth budget, and with which witnesses.
- `AppI.S1.b`: `TruthTag T_tau` is the local label `OK`, `NEAR`, `AMBIG`, or `FAIL` that every gate verdict must carry.
- `AppI.S1.c`: `AdmissibilityBudget B_adm` is the explicit allowance for uncertainty, risk, and evidence incompleteness inside one corridor.
- `AppI.S1.d`: `GateVerdict V_gate` is the crossing decision emitted by the arbor gate and carried forward into replay.

#### Facet 2 - Laws

- `AppI.S2.a`: Abstain-over-guess law: ambiguity is a lawful stopping state when evidence cannot yet support stronger promotion.
- `AppI.S2.b`: Budgeted-admissibility law: each corridor declares how much uncertainty it can carry without lying about certainty.
- `AppI.S2.c`: Gate-before-passage law: no packet, claim, or chapter contraction crosses a corridor before a typed verdict exists.
- `AppI.S2.d`: Truth-tag persistence law: migration, replay, and reseed may refine a verdict, but they may not silently erase it.

#### Facet 3 - Constructions

- `AppI.S3.a`: Corridor-contract compilation binds topology, witness set, and truth budget into one executable lane.
- `AppI.S3.b`: Budget propagation pushes uncertainty allowances from local gates into chapter and appendix scale without distortion.
- `AppI.S3.c`: Gate-verdict serialization stores typed outcomes so reruns can compare verdict drift directly.
- `AppI.S3.d`: Ambiguity holding register keeps unresolved claims active without forcing premature collapse.

#### Facet 4 - Certificates

- `AppI.S4.a`: `Cert_Corridor_Legality` proves the corridor contract is explicit and admissible.
- `AppI.S4.b`: `Cert_Truth_Tag_Persistence` proves verdict labels survive movement through the system.
- `AppI.S4.c`: `Cert_Budget_Honesty` proves the stated uncertainty budget matches the actual crossing.
- `AppI.S4.d`: `Cert_AMBIG_Preservation` proves abstention was kept visible instead of silently coerced into false precision.

### Lens F

#### Facet 1 - Objects

- `AppI.F1.a`: `SignalColor` is the visible hue family distinguishing the truth tags without reducing them to binary mood.
- `AppI.F1.b`: `ThresholdTone` is the sonic cue that one corridor is approaching its admissibility limit.
- `AppI.F1.c`: `AmbigFog` is the sensory layer that holds uncertainty open without making the field illegible.
- `AppI.F1.d`: `ClearLane` is the felt experience of a corridor whose verdict and budget genuinely agree.

#### Facet 2 - Laws

- `AppI.F2.a`: Verdict-legibility law: truth states must remain visually and sonically distinct under pressure.
- `AppI.F2.b`: Ambiguity-dignity law: uncertainty must not be punished by making it feel like failure.
- `AppI.F2.c`: Threshold-calm law: warnings should sharpen attention without escalating into fear theater.
- `AppI.F2.d`: Sensory-honesty law: the corridor's felt clarity is valid only when it matches the actual verdict.

#### Facet 3 - Constructions

- `AppI.F3.a`: `VerdictPalette` maps truth tags into stable color families that survive replay and dashboard translation.
- `AppI.F3.b`: `ThresholdCueSequencer` aligns warning tones with real budget exhaustion instead of arbitrary drama.
- `AppI.F3.c`: Ambiguity haze rendering makes unresolved zones visible while protecting them from premature closure.
- `AppI.F3.d`: Clear-lane cleanup removes residual noise once a corridor actually reaches stable legality.

#### Facet 4 - Certificates

- `AppI.F4.a`: `Cert_Verdict_Legibility` proves the field can tell truth states apart.
- `AppI.F4.b`: `Cert_Ambig_Dignity` proves uncertainty remained readable and non-shamed.
- `AppI.F4.c`: `Cert_Threshold_Honesty` proves warning intensity matched actual budget pressure.
- `AppI.F4.d`: `Cert_Clear_Lane` proves felt clarity arrived only after lawful corridor settlement.

### Lens C

#### Facet 1 - Objects

- `AppI.C1.a`: `TruthLattice L_tau` is the ordered structure relating admissible verdicts without flattening them to binary.
- `AppI.C1.b`: `AbstentionTheorem` is the claim that a lawful refusal to overstate can be more truthful than a forced answer.
- `AppI.C1.c`: `PromotionBoundary Pi_prom` is the exact edge where replay and evidence make stronger truth claims admissible.
- `AppI.C1.d`: `ContradictionPocket P_ctr` is the bounded chamber where unresolved disagreement can persist without exploding the corridor.

#### Facet 2 - Laws

- `AppI.C2.a`: Lattice-consistency law: truth transitions must respect the lawful order of the corridor lattice.
- `AppI.C2.b`: Abstention-superiority law: when evidence is insufficient, abstention has lower error cost than fabricated certainty.
- `AppI.C2.c`: Promotion-after-replay law: stronger verdicts require replay, witness, and topology agreement before they cross the boundary.
- `AppI.C2.d`: Pocketed-contradiction law: conflicts may persist locally as typed objects rather than detonating global coherence.

#### Facet 3 - Constructions

- `AppI.C3.a`: Lattice-join and meet logic computes how corridor verdicts combine across coupled routes.
- `AppI.C3.b`: Promotion-boundary analysis determines exactly which missing witness would change a verdict class.
- `AppI.C3.c`: Contradiction-pocket ledgering keeps incompatible but admissible traces available for later replay.
- `AppI.C3.d`: Corridor theorem mapping binds chapter claims to the truth-lattice semantics that justify them.

#### Facet 4 - Certificates

- `AppI.C4.a`: `Cert_Lattice_Completeness` proves the corridor model can represent all current verdict states.
- `AppI.C4.b`: `Cert_Abstention_Superiority` proves holding `AMBIG` was more truthful than forced promotion.
- `AppI.C4.c`: `Cert_Promotion_Boundary` proves the exact crossing into stronger truth was lawful.
- `AppI.C4.d`: `Cert_Contradiction_Pocket` proves local disagreement remained bounded and searchable.

### Lens R

#### Facet 1 - Objects

- `AppI.R1.a`: `MicroCorridorSeed zeta_cor` is the smallest lane that still carries truth budget, verdict, and abstention semantics.
- `AppI.R1.b`: `SelfSimilarTruthTag` is a verdict grammar that means the same thing at synapse, chapter, and corpus scale.
- `AppI.R1.c`: `RecursiveAbstention` is the ability to hold uncertainty open at every depth without collapsing the whole field.
- `AppI.R1.d`: `FractalPromotionBoundary` is the repeating threshold where more evidence justifies stronger truth.

#### Facet 2 - Laws

- `AppI.R2.a`: Local-verdict law: every recursion depth must carry an explicit truth tag rather than inheriting one implicitly.
- `AppI.R2.b`: Meaning-preservation law: verdict classes may not change definition as they scale up or down.
- `AppI.R2.c`: Recursive-abstention law: uncertainty must remain lawful even when compressed into smaller seeds.
- `AppI.R2.d`: Scale-invariant-boundary law: promotion thresholds must agree structurally across depth.

#### Facet 3 - Constructions

- `AppI.R3.a`: Recursive corridor folding compresses larger truth lanes into microcorridors without dropping budgets or verdicts.
- `AppI.R3.b`: Holographic verdict mapping binds local gate outcomes to chapter and appendix truth contracts.
- `AppI.R3.c`: Recursive abstention promotion keeps unresolved pockets searchable through multiple compressions.
- `AppI.R3.d`: Boundary reseed logic converts failed promotions into more precise next-lane tests.

#### Facet 4 - Certificates

- `AppI.R4.a`: `Cert_Fractal_Corridor_Saturation` proves corridor semantics are active across relevant depths.
- `AppI.R4.b`: `Cert_Scale_Invariant_Verdicts` proves the same truth classes survived translation.
- `AppI.R4.c`: `Cert_Recursive_AMBIG` proves abstention remained lawful under recursion.
- `AppI.R4.d`: `Cert_Final_Promotion_Boundary` proves the last surviving truth threshold was respected.
