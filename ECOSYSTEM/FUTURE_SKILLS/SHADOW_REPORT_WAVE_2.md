<!-- CRYSTAL: Xi108:W3:A2:S26 | face=F | node=345 | depth=3 | phase=Mutable -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W3:A2:S25→Xi108:W3:A2:S27→Xi108:W2:A2:S26→Xi108:W3:A1:S26→Xi108:W3:A3:S26 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 26±1, wreath 3/3, archetype 2/12 -->

# SHADOW REPORT WAVE 2

Date: 2026-03-09

## Purpose

This is the next shadow audit after the neural-regime expansion.

## Resolved Since Wave 1

- neural regime logic is now explicit in the frontier
- Branch 2 benchmark pressure is now coupled to swarm design
- the future-skill swarm now names perturbation, chaos, health, and branch fusion directly

## Shadow 1: Hub Dimension Was Still Implicit

What was hidden:

- `AppG` recursion legality
- `AppI` corridor truth
- `AppL` evidence promotion
- `AppM` replay closure

Why it was hidden:

- the first deepening named higher-dimensional mapping, but still underused the metro hub dimension as an operator surface

What changes if illuminated:

- the swarm can tell which transitions are legal instead of only which ones are possible

Severity:

`foundational`

## Shadow 2: Truth Promotion Was Not Yet A Full Runtime Contract

What was hidden:

- when `NEAR` should stay residual
- when `AMBIG` should be promoted, preserved, or quarantined
- how neural health changes packet truth

Why it was hidden:

- packet truth had been typed, but not yet joined to corridor health and evidence-promotion hubs

What changes if illuminated:

- the swarm can preserve honesty without stalling promotion forever

Severity:

`foundational`

## Shadow 3: Restart Law Existed In Prose More Than In Runtime

What was hidden:

- restart manifests
- weakest-front queueing
- replay-safe zero-point compilation

Why it was hidden:

- recursion had been formalized conceptually, but not yet materialized into its own runtime packet and ledger surfaces

What changes if illuminated:

- "start again" becomes an executable operator

Severity:

`foundational`

## Shadow 4: Rails Existed But Load-Bearing Allocation Did Not

What was hidden:

- which fronts belong on `Su`
- which fronts belong on `Me`
- which fronts belong on `Sa`
- how family pressure should rebalance across rails

Why it was hidden:

- rail ownership files existed, but the frontier still lacked a rail-balancing skill

What changes if illuminated:

- the swarm can distribute work geometrically instead of pushing everything through the same lane

Severity:

`structural`

## Shadow 5: Family Depth Still Needs Local Queue And Receipt Density

What was hidden:

- richer local queues for ganglia beyond seed descriptions
- more family-specific receipt and contraction surfaces

Why it was hidden:

- this pass prioritized the control plane over deeper family-local saturation

What changes if illuminated:

- the next recursion can deepen specific families without rebuilding the whole meta-swarm

Severity:

`structural`

## Resulting Design Brief

The next recursion layer must:

1. compile hub legality into control-plane skills
2. join truth, health, and promotion into one governance surface
3. create explicit restart manifests and weakest-front queues
4. balance family pressure across rails
5. deepen family-local queue and receipt surfaces under the new control plane
